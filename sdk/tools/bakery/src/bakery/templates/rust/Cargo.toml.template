[package]
name = "$name"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
inferlet = $inferlet_dep
serde = { version = "1.0", features = ["derive"] }

[profile.release]
lto = true
