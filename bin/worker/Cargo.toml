[package]
name = "pie-worker-bin"
version = "0.1.0"
edition = "2024"
authors = ["In Gim <in.gim@icloud.com>"]
description = "Pie worker daemon — thin bin shell composing the `startup` process skeleton with the `pie-worker` role library (Seam 3 of the build/packaging refactor)."
license = "Apache-2.0"
repository = "https://github.com/pie-project/pie"

# Binary name is the user-facing command; the package is `pie-worker-bin` to
# disambiguate from the `pie-worker` role library it depends on.
[[bin]]
name = "pie-worker"
path = "src/main.rs"

[dependencies]
# Shared process skeleton (imports as `startup`): CLI flag skeleton, config
# sourcing → String, tracing + /metrics, signals/panic/exit-code lifecycle.
pie-env = { path = "../env" }
# The worker role library: `Config::parse` + async `run` + `WorkerHandle::shutdown`.
pie-worker = { path = "../../worker" }
anyhow = "1"
clap = { version = "4.6", features = ["derive"] }
# This bin owns the runtime (Model A — the skeleton is runtime-agnostic).
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
