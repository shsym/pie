// GENERATED by `cargo run -p pie-forward --bin emit-cuda` — DO NOT EDIT.
// The static C++ form of the lowered qwen3_5 hybrid class traces
// (north-star-dsl.md rung 3, second family): one statement per traced
// op, choices already made at trace time. Included by the qwen3_5
// declared_forward.cpp inside its namespace; regenerate after any
// change to the declaration, the facts below, or the emitter, and
// re-run the A/B battery. Decode + prefill only; the MTP service
// classes stay on the interpreter walk.
//
// Emitted from: qwen3_5/l24/int4/v248320/te1/nv1/ah1024/aqh8/akvh2/ahd256/arot64/afq0/gkh16/gvh16/gkd128/gvd128/gck4/gfi0/moe0/di3584/sb1/wt0/wtm64/cm0/vs1

constexpr const char* kQ35GeneratedDigest_qwen3_5_0_8b =
    "qwen3_5/l24/int4/v248320/te1/nv1/ah1024/aqh8/akvh2/ahd256/arot64/afq0/gkh16/gvh16/gkd128/gvd128/gck4/gfi0/moe0/di3584/sb1/wt0/wtm64/cm0/vs1";

inline void generated_qwen35_decode_qwen3_5_0_8b(
const Qwen3_5Weights& w,
const HfConfig& cfg,
const Qwen3_5ForwardCfg& fwd_cfg,
const Qwen3_5PlanState& plan_state,
Workspace& ws,
Qwen3_5LinearAttnWorkspace& la,
KvCache& cache,
RecurrentStateCache& state_cache,
AttentionWorkspace& attn_ws,
ops::CublasHandle& cublas,
const std::int32_t* token_ids,
const std::int32_t* positions,
const std::uint32_t* qo_indptr,
const std::uint32_t* kv_page_indices,
const std::uint32_t* kv_page_indptr,
const std::uint32_t* kv_last_page_lens,
const std::uint32_t* qo_indptr_h,
const std::uint32_t* kv_page_indptr_h,
int total_tokens,
int num_requests,
const std::uint32_t* w_page_d,
const std::uint32_t* w_off_d,
const std::uint8_t* row_valid_d,
bool has_write_desc,
const std::int32_t* slot_ids_h,
const std::uint8_t* is_fresh_h,
const std::int32_t* slot_ids_d,
const std::uint8_t* is_fresh_d,
const std::int32_t* logit_row_indices_d,
int num_logit_rows,
const StageHooks* hooks)
{
    // Locals mirror the interpreter's preamble.
    const int N = total_tokens;
    const int R = num_requests;
    const int H = cfg.hidden_size;
    const int num_q_heads = cfg.num_attention_heads;
    const int num_kv_heads = cfg.num_key_value_heads;
    const int d = cfg.head_dim;
    const int Hq = num_q_heads * d;
    const int Hk = num_kv_heads * d;
    const int I = cfg.intermediate_size;
    const int K_h = cfg.linear_num_key_heads;
    const int V_h = cfg.linear_num_value_heads;
    const int K_d = cfg.linear_key_head_dim;
    const int V_d = cfg.linear_value_head_dim;
    const int K_dim = K_h * K_d;
    const int V_dim = V_h * V_d;
    const int conv_dim = 2 * K_dim + V_dim;
    const int conv_K = cfg.linear_conv_kernel_dim;
    const float eps = cfg.rms_norm_eps;
    cudaStream_t stream = cublas.stream();
    (void)fwd_cfg; (void)attn_ws; (void)qo_indptr_h;
    (void)kv_page_indptr_h; (void)K_dim;
    if (std::getenv("PIE_DECLARED_FORWARD_TRACE")) {
        std::fprintf(stderr,
                     "[declared-qwen35-generated] N=%d R=%d decode=1\n",
                     N, R);
    }

    // Per-slot reset for freshly (re)assigned rs slots — the
    // interpreter's reset stage, verbatim (no commit-advance
    // arm: the service classes stay on the interpreter walk).
    if (slot_ids_h != nullptr && is_fresh_h != nullptr) {
        if (std::any_of(is_fresh_h, is_fresh_h + R,
                        [](auto fresh) { return fresh != 0; })) {
            if (slot_ids_d != nullptr && is_fresh_d != nullptr) {
                state_cache.reset_slots_if_fresh(
                    slot_ids_d, is_fresh_d, R, stream);
            } else {
                for (int r = 0; r < R; ++r) {
                    if (is_fresh_h[r]) {
                        state_cache.reset_slot(slot_ids_h[r], stream);
                    }
                }
            }
        }
    }

    const ops::DecodePlanCache* decode_plan =
        plan_state.decode_plan ? plan_state.decode_plan.get() : nullptr;
    const ops::PrefillPlanCache* prefill_plan =
        (plan_state.use_prefill_plan && plan_state.prefill_plan)
            ? plan_state.prefill_plan.get()
            : nullptr;
    const auto slot_stride = static_cast<long long>(
        state_cache.recurrent_slot_stride_floats());
    const bool write_state = !state_cache.verify_frozen();
    (void)write_state;
    const float* q_recur_full = la.q_pre.data();
    const float* k_recur_full = la.k_pre.data();
    (void)q_recur_full; (void)k_recur_full;

    kernels::launch_embed_bf16(
        token_ids, require(w.embed, "embed")->data(), ws.y.data(),
        N, H, cfg.vocab_size, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].attn_norm_pre, "layer.0.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_qkv, "layer.0.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_z, "layer.0.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_a, "layer.0.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_b, "layer.0.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[0];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(0, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[0].la_A_log_fp32,
        require(w.layers[0].la_dt_bias, "layer.0.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(0, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[0].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[0].la_out_proj, "layer.0.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].mlp_norm_pre, "layer.0.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[0].gate_up_proj_fused, "layer.0.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[0].down_proj, "layer.0.down"), w.layers[0].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].attn_norm_pre, "layer.1.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_qkv, "layer.1.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_z, "layer.1.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_a, "layer.1.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_b, "layer.1.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[1];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(1, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[1].la_A_log_fp32,
        require(w.layers[1].la_dt_bias, "layer.1.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(1, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[1].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[1].la_out_proj, "layer.1.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].mlp_norm_pre, "layer.1.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[1].gate_up_proj_fused, "layer.1.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[1].down_proj, "layer.1.down"), w.layers[1].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].attn_norm_pre, "layer.2.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_qkv, "layer.2.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_z, "layer.2.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_a, "layer.2.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_b, "layer.2.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[2];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(2, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[2].la_A_log_fp32,
        require(w.layers[2].la_dt_bias, "layer.2.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(2, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[2].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[2].la_out_proj, "layer.2.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].mlp_norm_pre, "layer.2.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[2].gate_up_proj_fused, "layer.2.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[2].down_proj, "layer.2.down"), w.layers[2].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].attn_norm_pre, "layer.3.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_q_proj, "layer.3.q_proj"), w.layers[3].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_k_proj, "layer.3.k_proj"), w.layers[3].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_v_proj, "layer.3.v_proj"), w.layers[3].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[3].fa_q_norm, "layer.3.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[3].fa_k_norm, "layer.3.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (decode_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no decode plan");
    }
    { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
      ops::dispatch_attention_flashinfer_decode(
          *decode_plan,
          ws.q.data(), kv_view, ws.attn_out.data(),
          kv_page_indices, kv_page_indptr, kv_last_page_lens,
          attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[3].fa_o_proj, "layer.3.o_proj"), w.layers[3].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].mlp_norm_pre, "layer.3.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[3].gate_up_proj_fused, "layer.3.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[3].down_proj, "layer.3.down"), w.layers[3].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].attn_norm_pre, "layer.4.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_qkv, "layer.4.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_z, "layer.4.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_a, "layer.4.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_b, "layer.4.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[4];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(4, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[4].la_A_log_fp32,
        require(w.layers[4].la_dt_bias, "layer.4.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(4, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[4].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[4].la_out_proj, "layer.4.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].mlp_norm_pre, "layer.4.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[4].gate_up_proj_fused, "layer.4.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[4].down_proj, "layer.4.down"), w.layers[4].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].attn_norm_pre, "layer.5.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_qkv, "layer.5.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_z, "layer.5.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_a, "layer.5.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_b, "layer.5.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[5];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(5, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[5].la_A_log_fp32,
        require(w.layers[5].la_dt_bias, "layer.5.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(5, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[5].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[5].la_out_proj, "layer.5.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].mlp_norm_pre, "layer.5.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[5].gate_up_proj_fused, "layer.5.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[5].down_proj, "layer.5.down"), w.layers[5].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].attn_norm_pre, "layer.6.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_qkv, "layer.6.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_z, "layer.6.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_a, "layer.6.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_b, "layer.6.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[6];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(6, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[6].la_A_log_fp32,
        require(w.layers[6].la_dt_bias, "layer.6.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(6, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[6].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[6].la_out_proj, "layer.6.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].mlp_norm_pre, "layer.6.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[6].gate_up_proj_fused, "layer.6.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[6].down_proj, "layer.6.down"), w.layers[6].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].attn_norm_pre, "layer.7.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_q_proj, "layer.7.q_proj"), w.layers[7].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_k_proj, "layer.7.k_proj"), w.layers[7].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_v_proj, "layer.7.v_proj"), w.layers[7].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[7].fa_q_norm, "layer.7.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[7].fa_k_norm, "layer.7.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (decode_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no decode plan");
    }
    { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
      ops::dispatch_attention_flashinfer_decode(
          *decode_plan,
          ws.q.data(), kv_view, ws.attn_out.data(),
          kv_page_indices, kv_page_indptr, kv_last_page_lens,
          attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[7].fa_o_proj, "layer.7.o_proj"), w.layers[7].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].mlp_norm_pre, "layer.7.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[7].gate_up_proj_fused, "layer.7.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[7].down_proj, "layer.7.down"), w.layers[7].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].attn_norm_pre, "layer.8.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_qkv, "layer.8.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_z, "layer.8.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_a, "layer.8.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_b, "layer.8.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[8];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(8, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[8].la_A_log_fp32,
        require(w.layers[8].la_dt_bias, "layer.8.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(8, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[8].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[8].la_out_proj, "layer.8.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].mlp_norm_pre, "layer.8.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[8].gate_up_proj_fused, "layer.8.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[8].down_proj, "layer.8.down"), w.layers[8].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].attn_norm_pre, "layer.9.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_qkv, "layer.9.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_z, "layer.9.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_a, "layer.9.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_b, "layer.9.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[9];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(9, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[9].la_A_log_fp32,
        require(w.layers[9].la_dt_bias, "layer.9.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(9, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[9].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[9].la_out_proj, "layer.9.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].mlp_norm_pre, "layer.9.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[9].gate_up_proj_fused, "layer.9.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[9].down_proj, "layer.9.down"), w.layers[9].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].attn_norm_pre, "layer.10.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_qkv, "layer.10.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_z, "layer.10.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_a, "layer.10.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_b, "layer.10.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[10];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(10, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[10].la_A_log_fp32,
        require(w.layers[10].la_dt_bias, "layer.10.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(10, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[10].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[10].la_out_proj, "layer.10.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].mlp_norm_pre, "layer.10.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[10].gate_up_proj_fused, "layer.10.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[10].down_proj, "layer.10.down"), w.layers[10].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].attn_norm_pre, "layer.11.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_q_proj, "layer.11.q_proj"), w.layers[11].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_k_proj, "layer.11.k_proj"), w.layers[11].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_v_proj, "layer.11.v_proj"), w.layers[11].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[11].fa_q_norm, "layer.11.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[11].fa_k_norm, "layer.11.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (decode_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no decode plan");
    }
    { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
      ops::dispatch_attention_flashinfer_decode(
          *decode_plan,
          ws.q.data(), kv_view, ws.attn_out.data(),
          kv_page_indices, kv_page_indptr, kv_last_page_lens,
          attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[11].fa_o_proj, "layer.11.o_proj"), w.layers[11].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].mlp_norm_pre, "layer.11.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[11].gate_up_proj_fused, "layer.11.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[11].down_proj, "layer.11.down"), w.layers[11].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].attn_norm_pre, "layer.12.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_qkv, "layer.12.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_z, "layer.12.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_a, "layer.12.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_b, "layer.12.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[12];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(12, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[12].la_A_log_fp32,
        require(w.layers[12].la_dt_bias, "layer.12.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(12, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[12].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[12].la_out_proj, "layer.12.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].mlp_norm_pre, "layer.12.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[12].gate_up_proj_fused, "layer.12.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[12].down_proj, "layer.12.down"), w.layers[12].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].attn_norm_pre, "layer.13.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_qkv, "layer.13.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_z, "layer.13.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_a, "layer.13.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_b, "layer.13.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[13];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(13, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[13].la_A_log_fp32,
        require(w.layers[13].la_dt_bias, "layer.13.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(13, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[13].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[13].la_out_proj, "layer.13.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].mlp_norm_pre, "layer.13.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[13].gate_up_proj_fused, "layer.13.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[13].down_proj, "layer.13.down"), w.layers[13].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].attn_norm_pre, "layer.14.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_qkv, "layer.14.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_z, "layer.14.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_a, "layer.14.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_b, "layer.14.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[14];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(14, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[14].la_A_log_fp32,
        require(w.layers[14].la_dt_bias, "layer.14.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(14, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[14].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[14].la_out_proj, "layer.14.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].mlp_norm_pre, "layer.14.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[14].gate_up_proj_fused, "layer.14.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[14].down_proj, "layer.14.down"), w.layers[14].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].attn_norm_pre, "layer.15.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_q_proj, "layer.15.q_proj"), w.layers[15].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_k_proj, "layer.15.k_proj"), w.layers[15].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_v_proj, "layer.15.v_proj"), w.layers[15].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[15].fa_q_norm, "layer.15.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[15].fa_k_norm, "layer.15.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (decode_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no decode plan");
    }
    { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
      ops::dispatch_attention_flashinfer_decode(
          *decode_plan,
          ws.q.data(), kv_view, ws.attn_out.data(),
          kv_page_indices, kv_page_indptr, kv_last_page_lens,
          attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[15].fa_o_proj, "layer.15.o_proj"), w.layers[15].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].mlp_norm_pre, "layer.15.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[15].gate_up_proj_fused, "layer.15.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[15].down_proj, "layer.15.down"), w.layers[15].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].attn_norm_pre, "layer.16.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_qkv, "layer.16.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_z, "layer.16.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_a, "layer.16.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_b, "layer.16.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[16];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(16, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[16].la_A_log_fp32,
        require(w.layers[16].la_dt_bias, "layer.16.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(16, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[16].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[16].la_out_proj, "layer.16.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].mlp_norm_pre, "layer.16.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[16].gate_up_proj_fused, "layer.16.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[16].down_proj, "layer.16.down"), w.layers[16].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].attn_norm_pre, "layer.17.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_qkv, "layer.17.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_z, "layer.17.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_a, "layer.17.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_b, "layer.17.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[17];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(17, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[17].la_A_log_fp32,
        require(w.layers[17].la_dt_bias, "layer.17.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(17, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[17].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[17].la_out_proj, "layer.17.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].mlp_norm_pre, "layer.17.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[17].gate_up_proj_fused, "layer.17.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[17].down_proj, "layer.17.down"), w.layers[17].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].attn_norm_pre, "layer.18.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_qkv, "layer.18.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_z, "layer.18.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_a, "layer.18.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_b, "layer.18.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[18];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(18, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[18].la_A_log_fp32,
        require(w.layers[18].la_dt_bias, "layer.18.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(18, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[18].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[18].la_out_proj, "layer.18.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].mlp_norm_pre, "layer.18.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[18].gate_up_proj_fused, "layer.18.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[18].down_proj, "layer.18.down"), w.layers[18].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].attn_norm_pre, "layer.19.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_q_proj, "layer.19.q_proj"), w.layers[19].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_k_proj, "layer.19.k_proj"), w.layers[19].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_v_proj, "layer.19.v_proj"), w.layers[19].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[19].fa_q_norm, "layer.19.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[19].fa_k_norm, "layer.19.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (decode_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no decode plan");
    }
    { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
      ops::dispatch_attention_flashinfer_decode(
          *decode_plan,
          ws.q.data(), kv_view, ws.attn_out.data(),
          kv_page_indices, kv_page_indptr, kv_last_page_lens,
          attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[19].fa_o_proj, "layer.19.o_proj"), w.layers[19].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].mlp_norm_pre, "layer.19.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[19].gate_up_proj_fused, "layer.19.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[19].down_proj, "layer.19.down"), w.layers[19].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].attn_norm_pre, "layer.20.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_qkv, "layer.20.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_z, "layer.20.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_a, "layer.20.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_b, "layer.20.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[20];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(20, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[20].la_A_log_fp32,
        require(w.layers[20].la_dt_bias, "layer.20.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(20, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[20].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[20].la_out_proj, "layer.20.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].mlp_norm_pre, "layer.20.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[20].gate_up_proj_fused, "layer.20.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[20].down_proj, "layer.20.down"), w.layers[20].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].attn_norm_pre, "layer.21.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_qkv, "layer.21.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_z, "layer.21.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_a, "layer.21.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_b, "layer.21.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[21];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(21, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[21].la_A_log_fp32,
        require(w.layers[21].la_dt_bias, "layer.21.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(21, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[21].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[21].la_out_proj, "layer.21.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].mlp_norm_pre, "layer.21.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[21].gate_up_proj_fused, "layer.21.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[21].down_proj, "layer.21.down"), w.layers[21].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].attn_norm_pre, "layer.22.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_qkv, "layer.22.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_z, "layer.22.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_a, "layer.22.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_b, "layer.22.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[22];
      kernels::launch_causal_conv1d_update_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          state_cache.conv_state(22, /*slot=*/0),
          slot_ids_d,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          la.mixed_qkv_post.data(),
          R, conv_dim, conv_K, stream); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[22].la_A_log_fp32,
        require(w.layers[22].la_dt_bias, "layer.22.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_recurrent_gated_delta_step_batched_state_bf16(
        q_recur_full, k_recur_full,
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(22, /*slot=*/0)),
        slot_ids_d, slot_stride,
        la.core_out.data(), R, V_h, K_d, V_d, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[22].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[22].la_out_proj, "layer.22.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].mlp_norm_pre, "layer.22.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[22].gate_up_proj_fused, "layer.22.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[22].down_proj, "layer.22.down"), w.layers[22].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].attn_norm_pre, "layer.23.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_q_proj, "layer.23.q_proj"), w.layers[23].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_k_proj, "layer.23.k_proj"), w.layers[23].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_v_proj, "layer.23.v_proj"), w.layers[23].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[23].fa_q_norm, "layer.23.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[23].fa_k_norm, "layer.23.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (decode_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no decode plan");
    }
    { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
      ops::dispatch_attention_flashinfer_decode(
          *decode_plan,
          ws.q.data(), kv_view, ws.attn_out.data(),
          kv_page_indices, kv_page_indptr, kv_last_page_lens,
          attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[23].fa_o_proj, "layer.23.o_proj"), w.layers[23].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].mlp_norm_pre, "layer.23.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[23].gate_up_proj_fused, "layer.23.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[23].down_proj, "layer.23.down"), w.layers[23].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.final_norm, "final_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    // The epilogue: final norm already landed ALL rows in
    // norm_x (its op above); gather compact rows, multiply,
    // and copy the normed hidden back to ws.y (MTP/state
    // plumbing) — the interpreter's arm verbatim.
    if (logit_row_indices_d != nullptr &&
        num_logit_rows > 0 &&
        num_logit_rows < N) {
        kernels::launch_gather_bf16_rows(
            static_cast<const std::uint16_t*>(ws.norm_x.data()),
            logit_row_indices_d,
            static_cast<std::uint16_t*>(ws.norm_y.data()),
            num_logit_rows, H, stream);
        ops::gemm_act_x_w(cublas.handle(),
            ws.norm_y.data(), *require(w.embed, "embed"),
            ws.logits.data(), num_logit_rows, cfg.vocab_size, H);
    } else {
        ops::gemm_act_x_w(cublas.handle(),
            ws.norm_x.data(), *require(w.embed, "embed"),
            ws.logits.data(), N, cfg.vocab_size, H);
    }
    CUDA_CHECK(cudaMemcpyAsync(
        ws.y.data(), ws.norm_x.data(),
        static_cast<std::size_t>(N) * H * sizeof(std::uint16_t),
        cudaMemcpyDeviceToDevice, stream));
}

inline void generated_qwen35_prefill_qwen3_5_0_8b(
const Qwen3_5Weights& w,
const HfConfig& cfg,
const Qwen3_5ForwardCfg& fwd_cfg,
const Qwen3_5PlanState& plan_state,
Workspace& ws,
Qwen3_5LinearAttnWorkspace& la,
KvCache& cache,
RecurrentStateCache& state_cache,
AttentionWorkspace& attn_ws,
ops::CublasHandle& cublas,
const std::int32_t* token_ids,
const std::int32_t* positions,
const std::uint32_t* qo_indptr,
const std::uint32_t* kv_page_indices,
const std::uint32_t* kv_page_indptr,
const std::uint32_t* kv_last_page_lens,
const std::uint32_t* qo_indptr_h,
const std::uint32_t* kv_page_indptr_h,
int total_tokens,
int num_requests,
const std::uint32_t* w_page_d,
const std::uint32_t* w_off_d,
const std::uint8_t* row_valid_d,
bool has_write_desc,
const std::int32_t* slot_ids_h,
const std::uint8_t* is_fresh_h,
const std::int32_t* slot_ids_d,
const std::uint8_t* is_fresh_d,
const std::int32_t* logit_row_indices_d,
int num_logit_rows,
const StageHooks* hooks)
{
    // Locals mirror the interpreter's preamble.
    const int N = total_tokens;
    const int R = num_requests;
    const int H = cfg.hidden_size;
    const int num_q_heads = cfg.num_attention_heads;
    const int num_kv_heads = cfg.num_key_value_heads;
    const int d = cfg.head_dim;
    const int Hq = num_q_heads * d;
    const int Hk = num_kv_heads * d;
    const int I = cfg.intermediate_size;
    const int K_h = cfg.linear_num_key_heads;
    const int V_h = cfg.linear_num_value_heads;
    const int K_d = cfg.linear_key_head_dim;
    const int V_d = cfg.linear_value_head_dim;
    const int K_dim = K_h * K_d;
    const int V_dim = V_h * V_d;
    const int conv_dim = 2 * K_dim + V_dim;
    const int conv_K = cfg.linear_conv_kernel_dim;
    const float eps = cfg.rms_norm_eps;
    cudaStream_t stream = cublas.stream();
    (void)fwd_cfg; (void)attn_ws; (void)qo_indptr_h;
    (void)kv_page_indptr_h; (void)K_dim;
    if (std::getenv("PIE_DECLARED_FORWARD_TRACE")) {
        std::fprintf(stderr,
                     "[declared-qwen35-generated] N=%d R=%d decode=0\n",
                     N, R);
    }

    // Per-slot reset for freshly (re)assigned rs slots — the
    // interpreter's reset stage, verbatim (no commit-advance
    // arm: the service classes stay on the interpreter walk).
    if (slot_ids_h != nullptr && is_fresh_h != nullptr) {
        if (std::any_of(is_fresh_h, is_fresh_h + R,
                        [](auto fresh) { return fresh != 0; })) {
            if (slot_ids_d != nullptr && is_fresh_d != nullptr) {
                state_cache.reset_slots_if_fresh(
                    slot_ids_d, is_fresh_d, R, stream);
            } else {
                for (int r = 0; r < R; ++r) {
                    if (is_fresh_h[r]) {
                        state_cache.reset_slot(slot_ids_h[r], stream);
                    }
                }
            }
        }
    } else {
        // Legacy null-slot prefill: reset all.
        state_cache.reset(stream);
    }

    const ops::DecodePlanCache* decode_plan =
        plan_state.decode_plan ? plan_state.decode_plan.get() : nullptr;
    const ops::PrefillPlanCache* prefill_plan =
        (plan_state.use_prefill_plan && plan_state.prefill_plan)
            ? plan_state.prefill_plan.get()
            : nullptr;
    const auto slot_stride = static_cast<long long>(
        state_cache.recurrent_slot_stride_floats());
    const bool write_state = !state_cache.verify_frozen();
    (void)write_state;
    const float* q_recur_full = la.q_pre.data();
    const float* k_recur_full = la.k_pre.data();
    (void)q_recur_full; (void)k_recur_full;

    kernels::launch_embed_bf16(
        token_ids, require(w.embed, "embed")->data(), ws.y.data(),
        N, H, cfg.vocab_size, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].attn_norm_pre, "layer.0.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_qkv, "layer.0.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_z, "layer.0.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_a, "layer.0.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_b, "layer.0.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[0];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(0, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[0].la_A_log_fp32,
        require(w.layers[0].la_dt_bias, "layer.0.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(0, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(0, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[0].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[0].la_out_proj, "layer.0.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].mlp_norm_pre, "layer.0.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[0].gate_up_proj_fused, "layer.0.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[0].down_proj, "layer.0.down"), w.layers[0].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].attn_norm_pre, "layer.1.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_qkv, "layer.1.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_z, "layer.1.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_a, "layer.1.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_b, "layer.1.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[1];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(1, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[1].la_A_log_fp32,
        require(w.layers[1].la_dt_bias, "layer.1.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(1, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(1, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[1].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[1].la_out_proj, "layer.1.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].mlp_norm_pre, "layer.1.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[1].gate_up_proj_fused, "layer.1.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[1].down_proj, "layer.1.down"), w.layers[1].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].attn_norm_pre, "layer.2.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_qkv, "layer.2.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_z, "layer.2.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_a, "layer.2.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_b, "layer.2.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[2];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(2, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[2].la_A_log_fp32,
        require(w.layers[2].la_dt_bias, "layer.2.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(2, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(2, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[2].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[2].la_out_proj, "layer.2.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].mlp_norm_pre, "layer.2.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[2].gate_up_proj_fused, "layer.2.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[2].down_proj, "layer.2.down"), w.layers[2].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].attn_norm_pre, "layer.3.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_q_proj, "layer.3.q_proj"), w.layers[3].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_k_proj, "layer.3.k_proj"), w.layers[3].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_v_proj, "layer.3.v_proj"), w.layers[3].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[3].fa_q_norm, "layer.3.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[3].fa_k_norm, "layer.3.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[3].fa_o_proj, "layer.3.o_proj"), w.layers[3].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].mlp_norm_pre, "layer.3.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[3].gate_up_proj_fused, "layer.3.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[3].down_proj, "layer.3.down"), w.layers[3].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].attn_norm_pre, "layer.4.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_qkv, "layer.4.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_z, "layer.4.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_a, "layer.4.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_b, "layer.4.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[4];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(4, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[4].la_A_log_fp32,
        require(w.layers[4].la_dt_bias, "layer.4.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(4, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(4, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[4].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[4].la_out_proj, "layer.4.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].mlp_norm_pre, "layer.4.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[4].gate_up_proj_fused, "layer.4.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[4].down_proj, "layer.4.down"), w.layers[4].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].attn_norm_pre, "layer.5.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_qkv, "layer.5.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_z, "layer.5.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_a, "layer.5.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_b, "layer.5.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[5];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(5, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[5].la_A_log_fp32,
        require(w.layers[5].la_dt_bias, "layer.5.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(5, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(5, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[5].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[5].la_out_proj, "layer.5.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].mlp_norm_pre, "layer.5.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[5].gate_up_proj_fused, "layer.5.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[5].down_proj, "layer.5.down"), w.layers[5].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].attn_norm_pre, "layer.6.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_qkv, "layer.6.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_z, "layer.6.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_a, "layer.6.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_b, "layer.6.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[6];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(6, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[6].la_A_log_fp32,
        require(w.layers[6].la_dt_bias, "layer.6.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(6, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(6, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[6].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[6].la_out_proj, "layer.6.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].mlp_norm_pre, "layer.6.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[6].gate_up_proj_fused, "layer.6.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[6].down_proj, "layer.6.down"), w.layers[6].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].attn_norm_pre, "layer.7.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_q_proj, "layer.7.q_proj"), w.layers[7].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_k_proj, "layer.7.k_proj"), w.layers[7].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_v_proj, "layer.7.v_proj"), w.layers[7].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[7].fa_q_norm, "layer.7.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[7].fa_k_norm, "layer.7.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[7].fa_o_proj, "layer.7.o_proj"), w.layers[7].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].mlp_norm_pre, "layer.7.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[7].gate_up_proj_fused, "layer.7.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[7].down_proj, "layer.7.down"), w.layers[7].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].attn_norm_pre, "layer.8.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_qkv, "layer.8.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_z, "layer.8.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_a, "layer.8.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_b, "layer.8.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[8];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(8, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[8].la_A_log_fp32,
        require(w.layers[8].la_dt_bias, "layer.8.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(8, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(8, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[8].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[8].la_out_proj, "layer.8.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].mlp_norm_pre, "layer.8.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[8].gate_up_proj_fused, "layer.8.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[8].down_proj, "layer.8.down"), w.layers[8].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].attn_norm_pre, "layer.9.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_qkv, "layer.9.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_z, "layer.9.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_a, "layer.9.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_b, "layer.9.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[9];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(9, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[9].la_A_log_fp32,
        require(w.layers[9].la_dt_bias, "layer.9.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(9, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(9, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[9].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[9].la_out_proj, "layer.9.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].mlp_norm_pre, "layer.9.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[9].gate_up_proj_fused, "layer.9.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[9].down_proj, "layer.9.down"), w.layers[9].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].attn_norm_pre, "layer.10.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_qkv, "layer.10.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_z, "layer.10.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_a, "layer.10.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_b, "layer.10.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[10];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(10, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[10].la_A_log_fp32,
        require(w.layers[10].la_dt_bias, "layer.10.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(10, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(10, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[10].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[10].la_out_proj, "layer.10.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].mlp_norm_pre, "layer.10.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[10].gate_up_proj_fused, "layer.10.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[10].down_proj, "layer.10.down"), w.layers[10].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].attn_norm_pre, "layer.11.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_q_proj, "layer.11.q_proj"), w.layers[11].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_k_proj, "layer.11.k_proj"), w.layers[11].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_v_proj, "layer.11.v_proj"), w.layers[11].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[11].fa_q_norm, "layer.11.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[11].fa_k_norm, "layer.11.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[11].fa_o_proj, "layer.11.o_proj"), w.layers[11].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].mlp_norm_pre, "layer.11.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[11].gate_up_proj_fused, "layer.11.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[11].down_proj, "layer.11.down"), w.layers[11].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].attn_norm_pre, "layer.12.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_qkv, "layer.12.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_z, "layer.12.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_a, "layer.12.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_b, "layer.12.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[12];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(12, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[12].la_A_log_fp32,
        require(w.layers[12].la_dt_bias, "layer.12.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(12, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(12, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[12].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[12].la_out_proj, "layer.12.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].mlp_norm_pre, "layer.12.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[12].gate_up_proj_fused, "layer.12.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[12].down_proj, "layer.12.down"), w.layers[12].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].attn_norm_pre, "layer.13.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_qkv, "layer.13.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_z, "layer.13.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_a, "layer.13.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_b, "layer.13.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[13];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(13, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[13].la_A_log_fp32,
        require(w.layers[13].la_dt_bias, "layer.13.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(13, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(13, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[13].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[13].la_out_proj, "layer.13.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].mlp_norm_pre, "layer.13.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[13].gate_up_proj_fused, "layer.13.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[13].down_proj, "layer.13.down"), w.layers[13].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].attn_norm_pre, "layer.14.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_qkv, "layer.14.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_z, "layer.14.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_a, "layer.14.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_b, "layer.14.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[14];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(14, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[14].la_A_log_fp32,
        require(w.layers[14].la_dt_bias, "layer.14.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(14, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(14, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[14].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[14].la_out_proj, "layer.14.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].mlp_norm_pre, "layer.14.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[14].gate_up_proj_fused, "layer.14.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[14].down_proj, "layer.14.down"), w.layers[14].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].attn_norm_pre, "layer.15.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_q_proj, "layer.15.q_proj"), w.layers[15].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_k_proj, "layer.15.k_proj"), w.layers[15].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_v_proj, "layer.15.v_proj"), w.layers[15].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[15].fa_q_norm, "layer.15.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[15].fa_k_norm, "layer.15.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[15].fa_o_proj, "layer.15.o_proj"), w.layers[15].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].mlp_norm_pre, "layer.15.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[15].gate_up_proj_fused, "layer.15.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[15].down_proj, "layer.15.down"), w.layers[15].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].attn_norm_pre, "layer.16.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_qkv, "layer.16.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_z, "layer.16.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_a, "layer.16.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_b, "layer.16.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[16];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(16, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[16].la_A_log_fp32,
        require(w.layers[16].la_dt_bias, "layer.16.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(16, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(16, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[16].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[16].la_out_proj, "layer.16.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].mlp_norm_pre, "layer.16.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[16].gate_up_proj_fused, "layer.16.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[16].down_proj, "layer.16.down"), w.layers[16].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].attn_norm_pre, "layer.17.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_qkv, "layer.17.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_z, "layer.17.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_a, "layer.17.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_b, "layer.17.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[17];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(17, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[17].la_A_log_fp32,
        require(w.layers[17].la_dt_bias, "layer.17.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(17, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(17, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[17].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[17].la_out_proj, "layer.17.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].mlp_norm_pre, "layer.17.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[17].gate_up_proj_fused, "layer.17.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[17].down_proj, "layer.17.down"), w.layers[17].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].attn_norm_pre, "layer.18.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_qkv, "layer.18.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_z, "layer.18.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_a, "layer.18.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_b, "layer.18.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[18];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(18, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[18].la_A_log_fp32,
        require(w.layers[18].la_dt_bias, "layer.18.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(18, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(18, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[18].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[18].la_out_proj, "layer.18.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].mlp_norm_pre, "layer.18.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[18].gate_up_proj_fused, "layer.18.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[18].down_proj, "layer.18.down"), w.layers[18].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].attn_norm_pre, "layer.19.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_q_proj, "layer.19.q_proj"), w.layers[19].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_k_proj, "layer.19.k_proj"), w.layers[19].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_v_proj, "layer.19.v_proj"), w.layers[19].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[19].fa_q_norm, "layer.19.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[19].fa_k_norm, "layer.19.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[19].fa_o_proj, "layer.19.o_proj"), w.layers[19].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].mlp_norm_pre, "layer.19.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[19].gate_up_proj_fused, "layer.19.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[19].down_proj, "layer.19.down"), w.layers[19].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].attn_norm_pre, "layer.20.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_qkv, "layer.20.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_z, "layer.20.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_a, "layer.20.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_b, "layer.20.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[20];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(20, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[20].la_A_log_fp32,
        require(w.layers[20].la_dt_bias, "layer.20.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(20, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(20, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[20].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[20].la_out_proj, "layer.20.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].mlp_norm_pre, "layer.20.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[20].gate_up_proj_fused, "layer.20.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[20].down_proj, "layer.20.down"), w.layers[20].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].attn_norm_pre, "layer.21.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_qkv, "layer.21.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_z, "layer.21.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_a, "layer.21.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_b, "layer.21.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[21];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(21, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[21].la_A_log_fp32,
        require(w.layers[21].la_dt_bias, "layer.21.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(21, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(21, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[21].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[21].la_out_proj, "layer.21.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].mlp_norm_pre, "layer.21.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[21].gate_up_proj_fused, "layer.21.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[21].down_proj, "layer.21.down"), w.layers[21].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].attn_norm_pre, "layer.22.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_qkv, "layer.22.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_z, "layer.22.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_a, "layer.22.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_b, "layer.22.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[22];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(22, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[22].la_A_log_fp32,
        require(w.layers[22].la_dt_bias, "layer.22.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(22, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(22, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[22].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[22].la_out_proj, "layer.22.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].mlp_norm_pre, "layer.22.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[22].gate_up_proj_fused, "layer.22.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[22].down_proj, "layer.22.down"), w.layers[22].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].attn_norm_pre, "layer.23.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_q_proj, "layer.23.q_proj"), w.layers[23].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_k_proj, "layer.23.k_proj"), w.layers[23].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_v_proj, "layer.23.v_proj"), w.layers[23].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[23].fa_q_norm, "layer.23.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[23].fa_k_norm, "layer.23.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[23].fa_o_proj, "layer.23.o_proj"), w.layers[23].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].mlp_norm_pre, "layer.23.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[23].gate_up_proj_fused, "layer.23.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[23].down_proj, "layer.23.down"), w.layers[23].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.final_norm, "final_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    // The epilogue: final norm already landed ALL rows in
    // norm_x (its op above); gather compact rows, multiply,
    // and copy the normed hidden back to ws.y (MTP/state
    // plumbing) — the interpreter's arm verbatim.
    if (logit_row_indices_d != nullptr &&
        num_logit_rows > 0 &&
        num_logit_rows < N) {
        kernels::launch_gather_bf16_rows(
            static_cast<const std::uint16_t*>(ws.norm_x.data()),
            logit_row_indices_d,
            static_cast<std::uint16_t*>(ws.norm_y.data()),
            num_logit_rows, H, stream);
        ops::gemm_act_x_w(cublas.handle(),
            ws.norm_y.data(), *require(w.embed, "embed"),
            ws.logits.data(), num_logit_rows, cfg.vocab_size, H);
    } else {
        ops::gemm_act_x_w(cublas.handle(),
            ws.norm_x.data(), *require(w.embed, "embed"),
            ws.logits.data(), N, cfg.vocab_size, H);
    }
    CUDA_CHECK(cudaMemcpyAsync(
        ws.y.data(), ws.norm_x.data(),
        static_cast<std::size_t>(N) * H * sizeof(std::uint16_t),
        cudaMemcpyDeviceToDevice, stream));
}

inline void generated_qwen35_state_only_qwen3_5_0_8b(
const Qwen3_5Weights& w,
const HfConfig& cfg,
const Qwen3_5ForwardCfg& fwd_cfg,
const Qwen3_5PlanState& plan_state,
Workspace& ws,
Qwen3_5LinearAttnWorkspace& la,
KvCache& cache,
RecurrentStateCache& state_cache,
AttentionWorkspace& attn_ws,
ops::CublasHandle& cublas,
const std::int32_t* token_ids,
const std::int32_t* positions,
const std::uint32_t* qo_indptr,
const std::uint32_t* kv_page_indices,
const std::uint32_t* kv_page_indptr,
const std::uint32_t* kv_last_page_lens,
const std::uint32_t* qo_indptr_h,
const std::uint32_t* kv_page_indptr_h,
int total_tokens,
int num_requests,
const std::uint32_t* w_page_d,
const std::uint32_t* w_off_d,
const std::uint8_t* row_valid_d,
bool has_write_desc,
const std::int32_t* slot_ids_h,
const std::uint8_t* is_fresh_h,
const std::int32_t* slot_ids_d,
const std::uint8_t* is_fresh_d,
const std::int32_t* logit_row_indices_d,
int num_logit_rows,
const StageHooks* hooks)
{
    // Locals mirror the interpreter's preamble.
    const int N = total_tokens;
    const int R = num_requests;
    const int H = cfg.hidden_size;
    const int num_q_heads = cfg.num_attention_heads;
    const int num_kv_heads = cfg.num_key_value_heads;
    const int d = cfg.head_dim;
    const int Hq = num_q_heads * d;
    const int Hk = num_kv_heads * d;
    const int I = cfg.intermediate_size;
    const int K_h = cfg.linear_num_key_heads;
    const int V_h = cfg.linear_num_value_heads;
    const int K_d = cfg.linear_key_head_dim;
    const int V_d = cfg.linear_value_head_dim;
    const int K_dim = K_h * K_d;
    const int V_dim = V_h * V_d;
    const int conv_dim = 2 * K_dim + V_dim;
    const int conv_K = cfg.linear_conv_kernel_dim;
    const float eps = cfg.rms_norm_eps;
    cudaStream_t stream = cublas.stream();
    (void)fwd_cfg; (void)attn_ws; (void)qo_indptr_h;
    (void)kv_page_indptr_h; (void)K_dim;
    if (std::getenv("PIE_DECLARED_FORWARD_TRACE")) {
        std::fprintf(stderr,
                     "[declared-qwen35-generated] N=%d R=%d decode=0\n",
                     N, R);
    }

    // Per-slot reset for freshly (re)assigned rs slots — the
    // interpreter's reset stage, verbatim (no commit-advance
    // arm: the service classes stay on the interpreter walk).
    if (slot_ids_h != nullptr && is_fresh_h != nullptr) {
        if (std::any_of(is_fresh_h, is_fresh_h + R,
                        [](auto fresh) { return fresh != 0; })) {
            if (slot_ids_d != nullptr && is_fresh_d != nullptr) {
                state_cache.reset_slots_if_fresh(
                    slot_ids_d, is_fresh_d, R, stream);
            } else {
                for (int r = 0; r < R; ++r) {
                    if (is_fresh_h[r]) {
                        state_cache.reset_slot(slot_ids_h[r], stream);
                    }
                }
            }
        }
    } else {
        // Legacy null-slot prefill: reset all.
        state_cache.reset(stream);
    }

    const ops::DecodePlanCache* decode_plan =
        plan_state.decode_plan ? plan_state.decode_plan.get() : nullptr;
    const ops::PrefillPlanCache* prefill_plan =
        (plan_state.use_prefill_plan && plan_state.prefill_plan)
            ? plan_state.prefill_plan.get()
            : nullptr;
    const auto slot_stride = static_cast<long long>(
        state_cache.recurrent_slot_stride_floats());
    const bool write_state = !state_cache.verify_frozen();
    (void)write_state;
    const float* q_recur_full = la.q_pre.data();
    const float* k_recur_full = la.k_pre.data();
    (void)q_recur_full; (void)k_recur_full;

    kernels::launch_embed_bf16(
        token_ids, require(w.embed, "embed")->data(), ws.y.data(),
        N, H, cfg.vocab_size, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].attn_norm_pre, "layer.0.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_qkv, "layer.0.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_z, "layer.0.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_a, "layer.0.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_b, "layer.0.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[0];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(0, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[0].la_A_log_fp32,
        require(w.layers[0].la_dt_bias, "layer.0.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(0, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(0, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[0].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[0].la_out_proj, "layer.0.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].mlp_norm_pre, "layer.0.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[0].gate_up_proj_fused, "layer.0.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[0].down_proj, "layer.0.down"), w.layers[0].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].attn_norm_pre, "layer.1.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_qkv, "layer.1.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_z, "layer.1.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_a, "layer.1.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_b, "layer.1.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[1];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(1, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[1].la_A_log_fp32,
        require(w.layers[1].la_dt_bias, "layer.1.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(1, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(1, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[1].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[1].la_out_proj, "layer.1.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].mlp_norm_pre, "layer.1.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[1].gate_up_proj_fused, "layer.1.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[1].down_proj, "layer.1.down"), w.layers[1].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].attn_norm_pre, "layer.2.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_qkv, "layer.2.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_z, "layer.2.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_a, "layer.2.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_b, "layer.2.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[2];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(2, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[2].la_A_log_fp32,
        require(w.layers[2].la_dt_bias, "layer.2.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(2, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(2, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[2].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[2].la_out_proj, "layer.2.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].mlp_norm_pre, "layer.2.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[2].gate_up_proj_fused, "layer.2.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[2].down_proj, "layer.2.down"), w.layers[2].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].attn_norm_pre, "layer.3.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_q_proj, "layer.3.q_proj"), w.layers[3].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_k_proj, "layer.3.k_proj"), w.layers[3].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_v_proj, "layer.3.v_proj"), w.layers[3].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[3].fa_q_norm, "layer.3.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[3].fa_k_norm, "layer.3.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[3].fa_o_proj, "layer.3.o_proj"), w.layers[3].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].mlp_norm_pre, "layer.3.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[3].gate_up_proj_fused, "layer.3.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[3].down_proj, "layer.3.down"), w.layers[3].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].attn_norm_pre, "layer.4.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_qkv, "layer.4.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_z, "layer.4.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_a, "layer.4.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_b, "layer.4.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[4];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(4, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[4].la_A_log_fp32,
        require(w.layers[4].la_dt_bias, "layer.4.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(4, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(4, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[4].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[4].la_out_proj, "layer.4.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].mlp_norm_pre, "layer.4.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[4].gate_up_proj_fused, "layer.4.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[4].down_proj, "layer.4.down"), w.layers[4].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].attn_norm_pre, "layer.5.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_qkv, "layer.5.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_z, "layer.5.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_a, "layer.5.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_b, "layer.5.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[5];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(5, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[5].la_A_log_fp32,
        require(w.layers[5].la_dt_bias, "layer.5.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(5, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(5, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[5].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[5].la_out_proj, "layer.5.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].mlp_norm_pre, "layer.5.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[5].gate_up_proj_fused, "layer.5.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[5].down_proj, "layer.5.down"), w.layers[5].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].attn_norm_pre, "layer.6.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_qkv, "layer.6.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_z, "layer.6.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_a, "layer.6.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_b, "layer.6.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[6];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(6, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[6].la_A_log_fp32,
        require(w.layers[6].la_dt_bias, "layer.6.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(6, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(6, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[6].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[6].la_out_proj, "layer.6.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].mlp_norm_pre, "layer.6.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[6].gate_up_proj_fused, "layer.6.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[6].down_proj, "layer.6.down"), w.layers[6].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].attn_norm_pre, "layer.7.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_q_proj, "layer.7.q_proj"), w.layers[7].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_k_proj, "layer.7.k_proj"), w.layers[7].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_v_proj, "layer.7.v_proj"), w.layers[7].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[7].fa_q_norm, "layer.7.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[7].fa_k_norm, "layer.7.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[7].fa_o_proj, "layer.7.o_proj"), w.layers[7].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].mlp_norm_pre, "layer.7.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[7].gate_up_proj_fused, "layer.7.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[7].down_proj, "layer.7.down"), w.layers[7].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].attn_norm_pre, "layer.8.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_qkv, "layer.8.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_z, "layer.8.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_a, "layer.8.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_b, "layer.8.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[8];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(8, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[8].la_A_log_fp32,
        require(w.layers[8].la_dt_bias, "layer.8.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(8, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(8, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[8].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[8].la_out_proj, "layer.8.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].mlp_norm_pre, "layer.8.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[8].gate_up_proj_fused, "layer.8.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[8].down_proj, "layer.8.down"), w.layers[8].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].attn_norm_pre, "layer.9.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_qkv, "layer.9.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_z, "layer.9.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_a, "layer.9.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_b, "layer.9.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[9];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(9, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[9].la_A_log_fp32,
        require(w.layers[9].la_dt_bias, "layer.9.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(9, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(9, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[9].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[9].la_out_proj, "layer.9.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].mlp_norm_pre, "layer.9.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[9].gate_up_proj_fused, "layer.9.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[9].down_proj, "layer.9.down"), w.layers[9].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].attn_norm_pre, "layer.10.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_qkv, "layer.10.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_z, "layer.10.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_a, "layer.10.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_b, "layer.10.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[10];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(10, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[10].la_A_log_fp32,
        require(w.layers[10].la_dt_bias, "layer.10.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(10, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(10, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[10].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[10].la_out_proj, "layer.10.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].mlp_norm_pre, "layer.10.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[10].gate_up_proj_fused, "layer.10.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[10].down_proj, "layer.10.down"), w.layers[10].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].attn_norm_pre, "layer.11.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_q_proj, "layer.11.q_proj"), w.layers[11].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_k_proj, "layer.11.k_proj"), w.layers[11].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_v_proj, "layer.11.v_proj"), w.layers[11].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[11].fa_q_norm, "layer.11.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[11].fa_k_norm, "layer.11.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[11].fa_o_proj, "layer.11.o_proj"), w.layers[11].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].mlp_norm_pre, "layer.11.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[11].gate_up_proj_fused, "layer.11.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[11].down_proj, "layer.11.down"), w.layers[11].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].attn_norm_pre, "layer.12.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_qkv, "layer.12.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_z, "layer.12.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_a, "layer.12.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_b, "layer.12.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[12];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(12, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[12].la_A_log_fp32,
        require(w.layers[12].la_dt_bias, "layer.12.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(12, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(12, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[12].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[12].la_out_proj, "layer.12.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].mlp_norm_pre, "layer.12.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[12].gate_up_proj_fused, "layer.12.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[12].down_proj, "layer.12.down"), w.layers[12].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].attn_norm_pre, "layer.13.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_qkv, "layer.13.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_z, "layer.13.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_a, "layer.13.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_b, "layer.13.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[13];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(13, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[13].la_A_log_fp32,
        require(w.layers[13].la_dt_bias, "layer.13.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(13, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(13, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[13].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[13].la_out_proj, "layer.13.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].mlp_norm_pre, "layer.13.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[13].gate_up_proj_fused, "layer.13.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[13].down_proj, "layer.13.down"), w.layers[13].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].attn_norm_pre, "layer.14.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_qkv, "layer.14.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_z, "layer.14.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_a, "layer.14.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_b, "layer.14.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[14];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(14, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[14].la_A_log_fp32,
        require(w.layers[14].la_dt_bias, "layer.14.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(14, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(14, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[14].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[14].la_out_proj, "layer.14.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].mlp_norm_pre, "layer.14.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[14].gate_up_proj_fused, "layer.14.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[14].down_proj, "layer.14.down"), w.layers[14].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].attn_norm_pre, "layer.15.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_q_proj, "layer.15.q_proj"), w.layers[15].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_k_proj, "layer.15.k_proj"), w.layers[15].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_v_proj, "layer.15.v_proj"), w.layers[15].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[15].fa_q_norm, "layer.15.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[15].fa_k_norm, "layer.15.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[15].fa_o_proj, "layer.15.o_proj"), w.layers[15].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].mlp_norm_pre, "layer.15.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[15].gate_up_proj_fused, "layer.15.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[15].down_proj, "layer.15.down"), w.layers[15].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].attn_norm_pre, "layer.16.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_qkv, "layer.16.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_z, "layer.16.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_a, "layer.16.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_b, "layer.16.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[16];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(16, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[16].la_A_log_fp32,
        require(w.layers[16].la_dt_bias, "layer.16.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(16, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(16, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[16].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[16].la_out_proj, "layer.16.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].mlp_norm_pre, "layer.16.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[16].gate_up_proj_fused, "layer.16.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[16].down_proj, "layer.16.down"), w.layers[16].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].attn_norm_pre, "layer.17.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_qkv, "layer.17.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_z, "layer.17.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_a, "layer.17.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_b, "layer.17.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[17];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(17, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[17].la_A_log_fp32,
        require(w.layers[17].la_dt_bias, "layer.17.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(17, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(17, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[17].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[17].la_out_proj, "layer.17.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].mlp_norm_pre, "layer.17.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[17].gate_up_proj_fused, "layer.17.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[17].down_proj, "layer.17.down"), w.layers[17].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].attn_norm_pre, "layer.18.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_qkv, "layer.18.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_z, "layer.18.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_a, "layer.18.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_b, "layer.18.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[18];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(18, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[18].la_A_log_fp32,
        require(w.layers[18].la_dt_bias, "layer.18.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(18, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(18, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[18].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[18].la_out_proj, "layer.18.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].mlp_norm_pre, "layer.18.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[18].gate_up_proj_fused, "layer.18.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[18].down_proj, "layer.18.down"), w.layers[18].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].attn_norm_pre, "layer.19.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_q_proj, "layer.19.q_proj"), w.layers[19].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_k_proj, "layer.19.k_proj"), w.layers[19].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_v_proj, "layer.19.v_proj"), w.layers[19].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[19].fa_q_norm, "layer.19.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[19].fa_k_norm, "layer.19.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[19].fa_o_proj, "layer.19.o_proj"), w.layers[19].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].mlp_norm_pre, "layer.19.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[19].gate_up_proj_fused, "layer.19.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[19].down_proj, "layer.19.down"), w.layers[19].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].attn_norm_pre, "layer.20.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_qkv, "layer.20.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_z, "layer.20.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_a, "layer.20.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_b, "layer.20.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[20];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(20, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[20].la_A_log_fp32,
        require(w.layers[20].la_dt_bias, "layer.20.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(20, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(20, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[20].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[20].la_out_proj, "layer.20.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].mlp_norm_pre, "layer.20.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[20].gate_up_proj_fused, "layer.20.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[20].down_proj, "layer.20.down"), w.layers[20].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].attn_norm_pre, "layer.21.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_qkv, "layer.21.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_z, "layer.21.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_a, "layer.21.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_b, "layer.21.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[21];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(21, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[21].la_A_log_fp32,
        require(w.layers[21].la_dt_bias, "layer.21.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(21, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(21, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[21].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[21].la_out_proj, "layer.21.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].mlp_norm_pre, "layer.21.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[21].gate_up_proj_fused, "layer.21.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[21].down_proj, "layer.21.down"), w.layers[21].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].attn_norm_pre, "layer.22.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_qkv, "layer.22.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_z, "layer.22.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_a, "layer.22.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_b, "layer.22.in_proj_b"),
        la.b.data(), N, V_h, H);
    { const auto& cl = w.layers[22];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(22, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[22].la_A_log_fp32,
        require(w.layers[22].la_dt_bias, "layer.22.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(22, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(22, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[22].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[22].la_out_proj, "layer.22.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].mlp_norm_pre, "layer.22.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[22].gate_up_proj_fused, "layer.22.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[22].down_proj, "layer.22.down"), w.layers[22].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].attn_norm_pre, "layer.23.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_q_proj, "layer.23.q_proj"), w.layers[23].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_k_proj, "layer.23.k_proj"), w.layers[23].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_v_proj, "layer.23.v_proj"), w.layers[23].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[23].fa_q_norm, "layer.23.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[23].fa_k_norm, "layer.23.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[23].fa_o_proj, "layer.23.o_proj"), w.layers[23].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].mlp_norm_pre, "layer.23.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[23].gate_up_proj_fused, "layer.23.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[23].down_proj, "layer.23.down"), w.layers[23].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
}

inline void generated_qwen35_frozen_verify_qwen3_5_0_8b(
const Qwen3_5Weights& w,
const HfConfig& cfg,
const Qwen3_5ForwardCfg& fwd_cfg,
const Qwen3_5PlanState& plan_state,
Workspace& ws,
Qwen3_5LinearAttnWorkspace& la,
KvCache& cache,
RecurrentStateCache& state_cache,
AttentionWorkspace& attn_ws,
ops::CublasHandle& cublas,
const std::int32_t* token_ids,
const std::int32_t* positions,
const std::uint32_t* qo_indptr,
const std::uint32_t* kv_page_indices,
const std::uint32_t* kv_page_indptr,
const std::uint32_t* kv_last_page_lens,
const std::uint32_t* qo_indptr_h,
const std::uint32_t* kv_page_indptr_h,
int total_tokens,
int num_requests,
const std::uint32_t* w_page_d,
const std::uint32_t* w_off_d,
const std::uint8_t* row_valid_d,
bool has_write_desc,
const std::int32_t* slot_ids_h,
const std::uint8_t* is_fresh_h,
const std::int32_t* slot_ids_d,
const std::uint8_t* is_fresh_d,
const std::int32_t* logit_row_indices_d,
int num_logit_rows,
const StageHooks* hooks)
{
    // Locals mirror the interpreter's preamble.
    const int N = total_tokens;
    const int R = num_requests;
    const int H = cfg.hidden_size;
    const int num_q_heads = cfg.num_attention_heads;
    const int num_kv_heads = cfg.num_key_value_heads;
    const int d = cfg.head_dim;
    const int Hq = num_q_heads * d;
    const int Hk = num_kv_heads * d;
    const int I = cfg.intermediate_size;
    const int K_h = cfg.linear_num_key_heads;
    const int V_h = cfg.linear_num_value_heads;
    const int K_d = cfg.linear_key_head_dim;
    const int V_d = cfg.linear_value_head_dim;
    const int K_dim = K_h * K_d;
    const int V_dim = V_h * V_d;
    const int conv_dim = 2 * K_dim + V_dim;
    const int conv_K = cfg.linear_conv_kernel_dim;
    const float eps = cfg.rms_norm_eps;
    cudaStream_t stream = cublas.stream();
    (void)fwd_cfg; (void)attn_ws; (void)qo_indptr_h;
    (void)kv_page_indptr_h; (void)K_dim;
    if (std::getenv("PIE_DECLARED_FORWARD_TRACE")) {
        std::fprintf(stderr,
                     "[declared-qwen35-generated] N=%d R=%d decode=0\n",
                     N, R);
    }

    // Per-slot reset for freshly (re)assigned rs slots — the
    // interpreter's reset stage, verbatim (no commit-advance
    // arm: the service classes stay on the interpreter walk).
    if (slot_ids_h != nullptr && is_fresh_h != nullptr) {
        if (std::any_of(is_fresh_h, is_fresh_h + R,
                        [](auto fresh) { return fresh != 0; })) {
            if (slot_ids_d != nullptr && is_fresh_d != nullptr) {
                state_cache.reset_slots_if_fresh(
                    slot_ids_d, is_fresh_d, R, stream);
            } else {
                for (int r = 0; r < R; ++r) {
                    if (is_fresh_h[r]) {
                        state_cache.reset_slot(slot_ids_h[r], stream);
                    }
                }
            }
        }
    } else {
        // Legacy null-slot prefill: reset all.
        state_cache.reset(stream);
    }

    const ops::DecodePlanCache* decode_plan =
        plan_state.decode_plan ? plan_state.decode_plan.get() : nullptr;
    const ops::PrefillPlanCache* prefill_plan =
        (plan_state.use_prefill_plan && plan_state.prefill_plan)
            ? plan_state.prefill_plan.get()
            : nullptr;
    const auto slot_stride = static_cast<long long>(
        state_cache.recurrent_slot_stride_floats());
    const bool write_state = !state_cache.verify_frozen();
    (void)write_state;
    const float* q_recur_full = la.q_pre.data();
    const float* k_recur_full = la.k_pre.data();
    (void)q_recur_full; (void)k_recur_full;

    kernels::launch_embed_bf16(
        token_ids, require(w.embed, "embed")->data(), ws.y.data(),
        N, H, cfg.vocab_size, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].attn_norm_pre, "layer.0.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_qkv, "layer.0.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_z, "layer.0.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_a, "layer.0.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[0].la_in_proj_b, "layer.0.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(0));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[0];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(0, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[0].la_A_log_fp32,
        require(w.layers[0].la_dt_bias, "layer.0.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(0, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(0, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[0].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[0].la_out_proj, "layer.0.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[0].mlp_norm_pre, "layer.0.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[0].gate_up_proj_fused, "layer.0.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[0].down_proj, "layer.0.down"), w.layers[0].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].attn_norm_pre, "layer.1.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_qkv, "layer.1.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_z, "layer.1.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_a, "layer.1.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[1].la_in_proj_b, "layer.1.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(1));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[1];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(1, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[1].la_A_log_fp32,
        require(w.layers[1].la_dt_bias, "layer.1.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(1, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(1, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[1].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[1].la_out_proj, "layer.1.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[1].mlp_norm_pre, "layer.1.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[1].gate_up_proj_fused, "layer.1.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[1].down_proj, "layer.1.down"), w.layers[1].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].attn_norm_pre, "layer.2.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_qkv, "layer.2.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_z, "layer.2.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_a, "layer.2.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[2].la_in_proj_b, "layer.2.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(2));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[2];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(2, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[2].la_A_log_fp32,
        require(w.layers[2].la_dt_bias, "layer.2.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(2, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(2, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[2].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[2].la_out_proj, "layer.2.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[2].mlp_norm_pre, "layer.2.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[2].gate_up_proj_fused, "layer.2.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[2].down_proj, "layer.2.down"), w.layers[2].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].attn_norm_pre, "layer.3.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_q_proj, "layer.3.q_proj"), w.layers[3].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_k_proj, "layer.3.k_proj"), w.layers[3].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[3].fa_v_proj, "layer.3.v_proj"), w.layers[3].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[3].fa_q_norm, "layer.3.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[3].fa_k_norm, "layer.3.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[3].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            3u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[3].fa_o_proj, "layer.3.o_proj"), w.layers[3].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[3].mlp_norm_pre, "layer.3.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[3].gate_up_proj_fused, "layer.3.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[3].down_proj, "layer.3.down"), w.layers[3].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].attn_norm_pre, "layer.4.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_qkv, "layer.4.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_z, "layer.4.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_a, "layer.4.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[4].la_in_proj_b, "layer.4.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(3));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[4];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(4, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[4].la_A_log_fp32,
        require(w.layers[4].la_dt_bias, "layer.4.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(4, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(4, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[4].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[4].la_out_proj, "layer.4.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[4].mlp_norm_pre, "layer.4.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[4].gate_up_proj_fused, "layer.4.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[4].down_proj, "layer.4.down"), w.layers[4].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].attn_norm_pre, "layer.5.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_qkv, "layer.5.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_z, "layer.5.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_a, "layer.5.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[5].la_in_proj_b, "layer.5.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(4));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[5];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(5, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[5].la_A_log_fp32,
        require(w.layers[5].la_dt_bias, "layer.5.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(5, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(5, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[5].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[5].la_out_proj, "layer.5.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[5].mlp_norm_pre, "layer.5.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[5].gate_up_proj_fused, "layer.5.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[5].down_proj, "layer.5.down"), w.layers[5].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].attn_norm_pre, "layer.6.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_qkv, "layer.6.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_z, "layer.6.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_a, "layer.6.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[6].la_in_proj_b, "layer.6.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(5));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[6];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(6, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[6].la_A_log_fp32,
        require(w.layers[6].la_dt_bias, "layer.6.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(6, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(6, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[6].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[6].la_out_proj, "layer.6.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[6].mlp_norm_pre, "layer.6.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[6].gate_up_proj_fused, "layer.6.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[6].down_proj, "layer.6.down"), w.layers[6].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].attn_norm_pre, "layer.7.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_q_proj, "layer.7.q_proj"), w.layers[7].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_k_proj, "layer.7.k_proj"), w.layers[7].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[7].fa_v_proj, "layer.7.v_proj"), w.layers[7].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[7].fa_q_norm, "layer.7.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[7].fa_k_norm, "layer.7.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[7].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            7u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[7].fa_o_proj, "layer.7.o_proj"), w.layers[7].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[7].mlp_norm_pre, "layer.7.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[7].gate_up_proj_fused, "layer.7.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[7].down_proj, "layer.7.down"), w.layers[7].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].attn_norm_pre, "layer.8.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_qkv, "layer.8.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_z, "layer.8.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_a, "layer.8.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[8].la_in_proj_b, "layer.8.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(6));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[8];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(8, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[8].la_A_log_fp32,
        require(w.layers[8].la_dt_bias, "layer.8.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(8, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(8, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[8].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[8].la_out_proj, "layer.8.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[8].mlp_norm_pre, "layer.8.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[8].gate_up_proj_fused, "layer.8.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[8].down_proj, "layer.8.down"), w.layers[8].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].attn_norm_pre, "layer.9.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_qkv, "layer.9.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_z, "layer.9.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_a, "layer.9.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[9].la_in_proj_b, "layer.9.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(7));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[9];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(9, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[9].la_A_log_fp32,
        require(w.layers[9].la_dt_bias, "layer.9.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(9, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(9, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[9].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[9].la_out_proj, "layer.9.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[9].mlp_norm_pre, "layer.9.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[9].gate_up_proj_fused, "layer.9.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[9].down_proj, "layer.9.down"), w.layers[9].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].attn_norm_pre, "layer.10.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_qkv, "layer.10.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_z, "layer.10.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_a, "layer.10.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[10].la_in_proj_b, "layer.10.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(8));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[10];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(10, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[10].la_A_log_fp32,
        require(w.layers[10].la_dt_bias, "layer.10.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(10, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(10, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[10].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[10].la_out_proj, "layer.10.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[10].mlp_norm_pre, "layer.10.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[10].gate_up_proj_fused, "layer.10.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[10].down_proj, "layer.10.down"), w.layers[10].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].attn_norm_pre, "layer.11.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_q_proj, "layer.11.q_proj"), w.layers[11].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_k_proj, "layer.11.k_proj"), w.layers[11].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[11].fa_v_proj, "layer.11.v_proj"), w.layers[11].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[11].fa_q_norm, "layer.11.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[11].fa_k_norm, "layer.11.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[11].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            11u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[11].fa_o_proj, "layer.11.o_proj"), w.layers[11].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[11].mlp_norm_pre, "layer.11.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[11].gate_up_proj_fused, "layer.11.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[11].down_proj, "layer.11.down"), w.layers[11].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].attn_norm_pre, "layer.12.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_qkv, "layer.12.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_z, "layer.12.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_a, "layer.12.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[12].la_in_proj_b, "layer.12.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(9));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[12];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(12, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[12].la_A_log_fp32,
        require(w.layers[12].la_dt_bias, "layer.12.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(12, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(12, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[12].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[12].la_out_proj, "layer.12.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[12].mlp_norm_pre, "layer.12.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[12].gate_up_proj_fused, "layer.12.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[12].down_proj, "layer.12.down"), w.layers[12].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].attn_norm_pre, "layer.13.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_qkv, "layer.13.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_z, "layer.13.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_a, "layer.13.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[13].la_in_proj_b, "layer.13.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(10));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[13];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(13, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[13].la_A_log_fp32,
        require(w.layers[13].la_dt_bias, "layer.13.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(13, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(13, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[13].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[13].la_out_proj, "layer.13.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[13].mlp_norm_pre, "layer.13.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[13].gate_up_proj_fused, "layer.13.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[13].down_proj, "layer.13.down"), w.layers[13].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].attn_norm_pre, "layer.14.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_qkv, "layer.14.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_z, "layer.14.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_a, "layer.14.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[14].la_in_proj_b, "layer.14.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(11));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[14];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(14, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[14].la_A_log_fp32,
        require(w.layers[14].la_dt_bias, "layer.14.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(14, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(14, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[14].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[14].la_out_proj, "layer.14.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[14].mlp_norm_pre, "layer.14.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[14].gate_up_proj_fused, "layer.14.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[14].down_proj, "layer.14.down"), w.layers[14].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].attn_norm_pre, "layer.15.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_q_proj, "layer.15.q_proj"), w.layers[15].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_k_proj, "layer.15.k_proj"), w.layers[15].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[15].fa_v_proj, "layer.15.v_proj"), w.layers[15].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[15].fa_q_norm, "layer.15.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[15].fa_k_norm, "layer.15.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[15].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            15u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[15].fa_o_proj, "layer.15.o_proj"), w.layers[15].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[15].mlp_norm_pre, "layer.15.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[15].gate_up_proj_fused, "layer.15.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[15].down_proj, "layer.15.down"), w.layers[15].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].attn_norm_pre, "layer.16.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_qkv, "layer.16.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_z, "layer.16.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_a, "layer.16.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[16].la_in_proj_b, "layer.16.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(12));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[16];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(16, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[16].la_A_log_fp32,
        require(w.layers[16].la_dt_bias, "layer.16.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(16, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(16, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[16].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[16].la_out_proj, "layer.16.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[16].mlp_norm_pre, "layer.16.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[16].gate_up_proj_fused, "layer.16.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[16].down_proj, "layer.16.down"), w.layers[16].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].attn_norm_pre, "layer.17.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_qkv, "layer.17.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_z, "layer.17.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_a, "layer.17.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[17].la_in_proj_b, "layer.17.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(13));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[17];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(17, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[17].la_A_log_fp32,
        require(w.layers[17].la_dt_bias, "layer.17.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(17, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(17, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[17].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[17].la_out_proj, "layer.17.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[17].mlp_norm_pre, "layer.17.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[17].gate_up_proj_fused, "layer.17.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[17].down_proj, "layer.17.down"), w.layers[17].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].attn_norm_pre, "layer.18.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_qkv, "layer.18.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_z, "layer.18.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_a, "layer.18.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[18].la_in_proj_b, "layer.18.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(14));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[18];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(18, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[18].la_A_log_fp32,
        require(w.layers[18].la_dt_bias, "layer.18.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(18, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(18, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[18].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[18].la_out_proj, "layer.18.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[18].mlp_norm_pre, "layer.18.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[18].gate_up_proj_fused, "layer.18.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[18].down_proj, "layer.18.down"), w.layers[18].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].attn_norm_pre, "layer.19.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_q_proj, "layer.19.q_proj"), w.layers[19].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_k_proj, "layer.19.k_proj"), w.layers[19].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[19].fa_v_proj, "layer.19.v_proj"), w.layers[19].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[19].fa_q_norm, "layer.19.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[19].fa_k_norm, "layer.19.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[19].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            19u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[19].fa_o_proj, "layer.19.o_proj"), w.layers[19].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[19].mlp_norm_pre, "layer.19.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[19].gate_up_proj_fused, "layer.19.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[19].down_proj, "layer.19.down"), w.layers[19].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].attn_norm_pre, "layer.20.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_qkv, "layer.20.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_z, "layer.20.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_a, "layer.20.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[20].la_in_proj_b, "layer.20.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(15));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[20];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(20, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[20].la_A_log_fp32,
        require(w.layers[20].la_dt_bias, "layer.20.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(20, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(20, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[20].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[20].la_out_proj, "layer.20.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[20].mlp_norm_pre, "layer.20.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[20].gate_up_proj_fused, "layer.20.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[20].down_proj, "layer.20.down"), w.layers[20].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].attn_norm_pre, "layer.21.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_qkv, "layer.21.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_z, "layer.21.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_a, "layer.21.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[21].la_in_proj_b, "layer.21.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(16));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[21];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(21, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[21].la_A_log_fp32,
        require(w.layers[21].la_dt_bias, "layer.21.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(21, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(21, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[21].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[21].la_out_proj, "layer.21.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[21].mlp_norm_pre, "layer.21.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[21].gate_up_proj_fused, "layer.21.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[21].down_proj, "layer.21.down"), w.layers[21].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].attn_norm_pre, "layer.22.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_qkv, "layer.22.in_proj_qkv"),
        la.mixed_qkv.data(), N, conv_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_z, "layer.22.in_proj_z"),
        la.z.data(), N, V_dim, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_a, "layer.22.in_proj_a"),
        la.a.data(), N, V_h, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        *require(w.layers[22].la_in_proj_b, "layer.22.in_proj_b"),
        la.b.data(), N, V_h, H);
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(17));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            stash, la.mixed_qkv.data(), n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + a_off, la.a.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            stash + b_off, la.b.data(), n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[22];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(22, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          /*commit_lens=*/nullptr); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[22].la_A_log_fp32,
        require(w.layers[22].la_dt_bias, "layer.22.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    if (N <= 0) {
        kernels::launch_chunk_gated_delta_prefill_batched_cached_state_bf16(
            q_recur_full, k_recur_full,
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(22, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, V_h, K_d, V_d, stream, write_state);
    } else {
        kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
            la.q_pre.data(), la.k_pre.data(),
            la.v_fp32.data(), la.g_log.data(), la.beta.data(),
            (state_cache.recurrent_state_raw(22, /*slot=*/0)),
            slot_ids_d, qo_indptr,
            slot_stride, la.core_out.data(),
            R, K_h, V_h, K_d, V_d, stream, write_state,
            /*commit_lens=*/nullptr);
    }
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_rmsnorm_gated_fp32_in_bf16(
        la.core_out.data(), la.z.data(), w.layers[22].la_norm_w_fp32,
        la.core_out_bf16.data(),
        N * V_h, V_d, /*eps=*/eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        la.core_out_bf16.data(),
        *require(w.layers[22].la_out_proj, "layer.22.o_proj"),
        ws.y.data(), N, H, V_dim, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[22].mlp_norm_pre, "layer.22.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[22].gate_up_proj_fused, "layer.22.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[22].down_proj, "layer.22.down"), w.layers[22].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].attn_norm_pre, "layer.23.attn_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_q_proj, "layer.23.q_proj"), w.layers[23].fa_q_proj_quant),
        la.fa_qg_packed.data(), N, 2 * Hq, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_k_proj, "layer.23.k_proj"), w.layers[23].fa_k_proj_quant),
        ws.k.data(), N, Hk, H);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        make_weight_view(require(w.layers[23].fa_v_proj, "layer.23.v_proj"), w.layers[23].fa_v_proj_quant),
        ws.v.data(), N, Hk, H);
    kernels::launch_split_q_gate_bf16(
        la.fa_qg_packed.data(), ws.q.data(), la.fa_gate.data(),
        N, num_q_heads, d, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.q.data(), require(w.layers[23].fa_q_norm, "layer.23.q_norm")->data(),
        ws.q.data(), N * num_q_heads, d, eps, stream);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.k.data(), require(w.layers[23].fa_k_norm, "layer.23.k_norm")->data(),
        ws.k.data(), N * num_kv_heads, d, eps, stream);
    kernels::launch_rope_partial_bf16(
        ws.q.data(), ws.k.data(), positions,
        N, num_q_heads, num_kv_heads,
        d, 64, cfg.rope_theta, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    if (has_write_desc) {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_explicit_bf16(
              kv_view, ws.k.data(), ws.v.data(),
              w_page_d, w_off_d, N, stream, row_valid_d); }
    } else {
        { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
          kernels::launch_write_kv_to_pages(
              kv_view, ws.k.data(), ws.v.data(),
              qo_indptr, kv_page_indices, kv_page_indptr,
              kv_last_page_lens, N, R, stream); }
    }
    if (prefill_plan == nullptr) {
        throw std::runtime_error(
            "generated qwen35: no prefill plan");
    }
    { auto kv_view = cache.layer_view(w.layers[23].kv_layer);
      ops::dispatch_attention_flashinfer_prefill_bf16(
          *prefill_plan,
          ws.q.data(), kv_view.k_bf16_pages, kv_view.v_bf16_pages,
          ws.attn_out.data(),
          qo_indptr, kv_page_indices, kv_page_indptr,
          kv_last_page_lens, attn_ws, stream); }
    kernels::launch_sigmoid_gate_inplace_bf16(
        ws.attn_out.data(), la.fa_gate.data(), N * Hq, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, ws.q.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(Hq),
            23u, stream, /*query_is_f32=*/false);
    }
    ops::gemm_act_x_w(cublas.handle(),
        ws.attn_out.data(),
        make_weight_view(require(w.layers[23].fa_o_proj, "layer.23.o_proj"), w.layers[23].fa_o_proj_quant),
        ws.y.data(), N, H, Hq, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.layers[23].mlp_norm_pre, "layer.23.mlp_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.norm_x.data(),
        ops::WeightView(*require(w.layers[23].gate_up_proj_fused, "layer.23.gate_up")),
        ws.gate_up_fused.data(), N, 2 * I, H);
    kernels::launch_chunked_swiglu_bf16(
        ws.gate_up_fused.data(), ws.gate.data(), N, I, stream);
    ops::gemm_act_x_w(cublas.handle(),
        ws.gate.data(),
        make_weight_view(require(w.layers[23].down_proj, "layer.23.down"), w.layers[23].down_proj_quant),
        ws.y.data(), N, H, I, 1.f);
    kernels::launch_rmsnorm_gemma_bf16(
        ws.y.data(), require(w.final_norm, "final_norm")->data(),
        ws.norm_x.data(), N, H, eps, stream);
    // The epilogue: final norm already landed ALL rows in
    // norm_x (its op above); gather compact rows, multiply,
    // and copy the normed hidden back to ws.y (MTP/state
    // plumbing) — the interpreter's arm verbatim.
    if (logit_row_indices_d != nullptr &&
        num_logit_rows > 0 &&
        num_logit_rows < N) {
        kernels::launch_gather_bf16_rows(
            static_cast<const std::uint16_t*>(ws.norm_x.data()),
            logit_row_indices_d,
            static_cast<std::uint16_t*>(ws.norm_y.data()),
            num_logit_rows, H, stream);
        ops::gemm_act_x_w(cublas.handle(),
            ws.norm_y.data(), *require(w.embed, "embed"),
            ws.logits.data(), num_logit_rows, cfg.vocab_size, H);
    } else {
        ops::gemm_act_x_w(cublas.handle(),
            ws.norm_x.data(), *require(w.embed, "embed"),
            ws.logits.data(), N, cfg.vocab_size, H);
    }
    CUDA_CHECK(cudaMemcpyAsync(
        ws.y.data(), ws.norm_x.data(),
        static_cast<std::size_t>(N) * H * sizeof(std::uint16_t),
        cudaMemcpyDeviceToDevice, stream));
}

inline void generated_qwen35_commit_advance_qwen3_5_0_8b(
const Qwen3_5Weights& w,
const HfConfig& cfg,
const Qwen3_5ForwardCfg& fwd_cfg,
const Qwen3_5PlanState& plan_state,
Workspace& ws,
Qwen3_5LinearAttnWorkspace& la,
KvCache& cache,
RecurrentStateCache& state_cache,
AttentionWorkspace& attn_ws,
ops::CublasHandle& cublas,
const std::int32_t* token_ids,
const std::int32_t* positions,
const std::uint32_t* qo_indptr,
const std::uint32_t* kv_page_indices,
const std::uint32_t* kv_page_indptr,
const std::uint32_t* kv_last_page_lens,
const std::uint32_t* qo_indptr_h,
const std::uint32_t* kv_page_indptr_h,
int total_tokens,
int num_requests,
const std::uint32_t* w_page_d,
const std::uint32_t* w_off_d,
const std::uint8_t* row_valid_d,
bool has_write_desc,
const std::int32_t* slot_ids_h,
const std::uint8_t* is_fresh_h,
const std::int32_t* slot_ids_d,
const std::uint8_t* is_fresh_d,
const std::int32_t* logit_row_indices_d,
int num_logit_rows,
const StageHooks* hooks,
    const std::int32_t* commit_lens)
{
    // Locals mirror the interpreter's preamble.
    const int N = total_tokens;
    const int R = num_requests;
    const int H = cfg.hidden_size;
    const int num_q_heads = cfg.num_attention_heads;
    const int num_kv_heads = cfg.num_key_value_heads;
    const int d = cfg.head_dim;
    const int Hq = num_q_heads * d;
    const int Hk = num_kv_heads * d;
    const int I = cfg.intermediate_size;
    const int K_h = cfg.linear_num_key_heads;
    const int V_h = cfg.linear_num_value_heads;
    const int K_d = cfg.linear_key_head_dim;
    const int V_d = cfg.linear_value_head_dim;
    const int K_dim = K_h * K_d;
    const int V_dim = V_h * V_d;
    const int conv_dim = 2 * K_dim + V_dim;
    const int conv_K = cfg.linear_conv_kernel_dim;
    const float eps = cfg.rms_norm_eps;
    cudaStream_t stream = cublas.stream();
    (void)fwd_cfg; (void)attn_ws; (void)qo_indptr_h;
    (void)kv_page_indptr_h; (void)K_dim;
    if (std::getenv("PIE_DECLARED_FORWARD_TRACE")) {
        std::fprintf(stderr,
                     "[declared-qwen35-generated] N=%d R=%d decode=0\n",
                     N, R);
    }

    // No reset: the commit replay advances the existing
    // committed state (the interpreter's commit arm).

    const ops::DecodePlanCache* decode_plan =
        plan_state.decode_plan ? plan_state.decode_plan.get() : nullptr;
    const ops::PrefillPlanCache* prefill_plan =
        (plan_state.use_prefill_plan && plan_state.prefill_plan)
            ? plan_state.prefill_plan.get()
            : nullptr;
    const auto slot_stride = static_cast<long long>(
        state_cache.recurrent_slot_stride_floats());
    const bool write_state = !state_cache.verify_frozen();
    (void)write_state;
    const float* q_recur_full = la.q_pre.data();
    const float* k_recur_full = la.k_pre.data();
    (void)q_recur_full; (void)k_recur_full;

    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(0));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[0];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(0, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[0].la_A_log_fp32,
        require(w.layers[0].la_dt_bias, "layer.0.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(0, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            0u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(1));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[1];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(1, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[1].la_A_log_fp32,
        require(w.layers[1].la_dt_bias, "layer.1.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(1, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            1u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(2));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[2];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(2, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[2].la_A_log_fp32,
        require(w.layers[2].la_dt_bias, "layer.2.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(2, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            2u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(3));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[4];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(4, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[4].la_A_log_fp32,
        require(w.layers[4].la_dt_bias, "layer.4.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(4, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            4u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(4));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[5];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(5, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[5].la_A_log_fp32,
        require(w.layers[5].la_dt_bias, "layer.5.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(5, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            5u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(5));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[6];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(6, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[6].la_A_log_fp32,
        require(w.layers[6].la_dt_bias, "layer.6.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(6, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            6u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(6));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[8];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(8, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[8].la_A_log_fp32,
        require(w.layers[8].la_dt_bias, "layer.8.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(8, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            8u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(7));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[9];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(9, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[9].la_A_log_fp32,
        require(w.layers[9].la_dt_bias, "layer.9.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(9, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            9u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(8));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[10];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(10, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[10].la_A_log_fp32,
        require(w.layers[10].la_dt_bias, "layer.10.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(10, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            10u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(9));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[12];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(12, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[12].la_A_log_fp32,
        require(w.layers[12].la_dt_bias, "layer.12.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(12, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            12u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(10));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[13];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(13, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[13].la_A_log_fp32,
        require(w.layers[13].la_dt_bias, "layer.13.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(13, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            13u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(11));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[14];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(14, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[14].la_A_log_fp32,
        require(w.layers[14].la_dt_bias, "layer.14.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(14, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            14u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(12));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[16];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(16, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[16].la_A_log_fp32,
        require(w.layers[16].la_dt_bias, "layer.16.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(16, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            16u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(13));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[17];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(17, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[17].la_A_log_fp32,
        require(w.layers[17].la_dt_bias, "layer.17.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(17, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            17u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(14));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[18];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(18, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[18].la_A_log_fp32,
        require(w.layers[18].la_dt_bias, "layer.18.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(18, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            18u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(15));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[20];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(20, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[20].la_A_log_fp32,
        require(w.layers[20].la_dt_bias, "layer.20.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(20, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            20u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(16));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[21];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(21, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[21].la_A_log_fp32,
        require(w.layers[21].la_dt_bias, "layer.21.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(21, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            21u, stream, /*query_is_f32=*/true);
    }
    if (!state_cache.verify_hidden_stash_enabled()) {
        throw std::runtime_error(
            "generated qwen35: stated stash op but the live "
            "stash is disabled (cross-check drift)");
    }
    {
        auto* stash = static_cast<std::uint16_t*>(
            state_cache.verify_hidden_stash_layer(17));
        const std::size_t stash_stride =
            static_cast<std::size_t>(state_cache.verify_stash_max_tokens());
        const std::size_t a_off = stash_stride * conv_dim;
        const std::size_t b_off =
            a_off + stash_stride * static_cast<std::size_t>(V_h);
        const std::size_t n_qkv =
            static_cast<std::size_t>(N) * conv_dim * sizeof(std::uint16_t);
        const std::size_t n_ab =
            static_cast<std::size_t>(N) * V_h * sizeof(std::uint16_t);
        CUDA_CHECK(cudaMemcpyAsync(
            la.mixed_qkv.data(), stash, n_qkv,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.a.data(), stash + a_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
        CUDA_CHECK(cudaMemcpyAsync(
            la.b.data(), stash + b_off, n_ab,
            cudaMemcpyDeviceToDevice, stream));
    }
    { const auto& cl = w.layers[22];
      kernels::launch_causal_conv1d_prefill_batched_bf16(
          la.mixed_qkv.data(), cl.la_conv1d_w->data(),
          cl.la_conv1d_b ? cl.la_conv1d_b->data() : nullptr,
          la.mixed_qkv_post.data(),
          state_cache.conv_state(22, /*slot=*/0),
          slot_ids_d, qo_indptr,
          static_cast<long long>(state_cache.conv_kernel()) *
              state_cache.conv_dim(),
          R, conv_dim, conv_K, stream, write_state,
          commit_lens); }
    kernels::launch_qwen_gdn_post_conv_prep_bf16(
        la.mixed_qkv_post.data(), la.a.data(), la.b.data(),
        w.layers[22].la_A_log_fp32,
        require(w.layers[22].la_dt_bias, "layer.22.dt_bias")->data(),
        la.q_pre.data(), la.k_pre.data(), la.v_fp32.data(),
        la.g_log.data(), la.beta.data(),
        N, K_h, V_h, K_d, V_d, conv_dim, stream);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttnProj, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
    kernels::launch_chunk_gated_delta_prefill_batched_state_bf16(
        la.q_pre.data(), la.k_pre.data(),
        la.v_fp32.data(), la.g_log.data(), la.beta.data(),
        (state_cache.recurrent_state_raw(22, /*slot=*/0)),
        slot_ids_d, qo_indptr,
        slot_stride, la.core_out.data(),
        R, K_h, V_h, K_d, V_d, stream, write_state,
        commit_lens);
    if (hooks != nullptr) {
        invoke_stage_hook(
            hooks, StageHookPoint::OnAttn, la.q_pre.data(),
            static_cast<std::uint32_t>(N),
            static_cast<std::uint32_t>(K_h * K_d),
            22u, stream, /*query_is_f32=*/true);
    }
}
