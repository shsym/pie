"""Per-driver copy of bridge_py's Python scaffolding.

See ``pie_driver_dev._bridge`` for the architectural rationale (vendored
from the legacy ``._bridge`` Python package as part of the
bridge consolidation Phase 3 — shared types live in the compiled
``pie_bridge`` wheel; driver-specific Python is copied per-driver).
"""
