--- a/ggml/include/ggml.h
+++ b/ggml/include/ggml.h
@@ -576,6 +576,8 @@
         GGML_OP_OPT_STEP_SGD,
 
         GGML_OP_GLU,
+
+        GGML_OP_PAGED_ATTN_EXT,
 
         GGML_OP_COUNT,
     };
@@ -2404,6 +2406,54 @@
     GGML_API void ggml_flash_attn_ext_add_sinks(
             struct ggml_tensor * a,
             struct ggml_tensor * sinks);
+
+    // pie patch: paged attention.
+    //
+    // Decode-only attention against a paged KV cache. The kernel itself is
+    // registered out-of-tree by the consumer (see
+    // ggml_cuda_register_paged_attn_ext below); when no kernel is registered
+    // the backend's supports_op returns false and callers are expected to
+    // fall back to materialize + ggml_flash_attn_ext. The op enum and
+    // constructor live here so any backend can build the graph node, but
+    // only backends with a registered kernel actually run it.
+    //
+    // Page-table inputs match the FlashInfer / vLLM convention:
+    //   page_indices    [total_pages_in_batch] I32 — flat list of page IDs
+    //                   used by all requests, concatenated per request
+    //   page_indptr     [n_req + 1]            I32 — prefix sums into
+    //                   page_indices; request r's pages are at
+    //                   page_indices[page_indptr[r] .. page_indptr[r+1])
+    //   last_page_lens  [n_req]                I32 — number of valid slots
+    //                   in each request's last page (1..page_size)
+    //
+    // Q layout matches flash_attn_ext: [head_dim, n_q_tokens, n_q_heads, n_req].
+    // For decode v1 callers, n_q_tokens must be 1.
+    //
+    // K and V are the layer's flat pool tensors:
+    //   k_pool / v_pool [head_dim * n_kv_heads, total_pages * page_size]
+    // Element layout matches FlashInfer's kNHD: contiguous along
+    // (head_dim, kv_head) within each (page, slot).
+    //
+    // sliding_window: -1 disables (full causal); >=0 clips attention to
+    // [seq_len - sliding_window, seq_len). The implementation handles SWA
+    // natively; no F16 mask tensor is required for the paged path.
+    //
+    // Returns: [head_dim, n_q_heads, n_q_tokens, n_req]   (permuted, matches
+    // flash_attn_ext). Output dtype follows q's dtype.
+    GGML_API struct ggml_tensor * ggml_paged_attn_ext(
+            struct ggml_context * ctx,
+            struct ggml_tensor  * q,
+            struct ggml_tensor  * k_pool,
+            struct ggml_tensor  * v_pool,
+            struct ggml_tensor  * page_indices,
+            struct ggml_tensor  * page_indptr,
+            struct ggml_tensor  * last_page_lens,
+            int                   page_size,
+            int                   head_dim,
+            int                   n_kv_heads,
+            int                   sliding_window,
+            float                 scale,
+            float                 softcap);
 
     // TODO: needs to be adapted to ggml_flash_attn_ext
     GGML_API struct ggml_tensor * ggml_flash_attn_back(
--- a/ggml/src/ggml.c
+++ b/ggml/src/ggml.c
@@ -1073,9 +1073,11 @@
     "OPT_STEP_SGD",
 
     "GLU",
+
+    "PAGED_ATTN_EXT",
 };
 
-static_assert(GGML_OP_COUNT == 96, "GGML_OP_COUNT != 96");
+static_assert(GGML_OP_COUNT == 97, "GGML_OP_COUNT != 97");
 
 static const char * GGML_OP_SYMBOL[GGML_OP_COUNT] = {
     "none",
@@ -1183,9 +1185,11 @@
     "sgd(x)",
 
     "glu(x)",
+
+    "paged_attn_ext(x)",
 };
 
-static_assert(GGML_OP_COUNT == 96, "GGML_OP_COUNT != 96");
+static_assert(GGML_OP_COUNT == 97, "GGML_OP_COUNT != 97");
 
 static_assert(GGML_OP_POOL_COUNT == 2, "GGML_OP_POOL_COUNT != 2");
 
@@ -5390,6 +5394,71 @@
     a->src[4] = sinks;
 }
 
+// ggml_paged_attn_ext (pie patch)
+//
+// Op-params layout (24 bytes; fits in GGML_MAX_OP_PARAMS = 64):
+//   [0] f32  scale
+//   [1] f32  softcap        (0.0 disables tanh-softcap)
+//   [2] i32  page_size
+//   [3] i32  head_dim       (per-layer; Gemma 4 sliding vs full differ)
+//   [4] i32  n_kv_heads     (per-layer)
+//   [5] i32  sliding_window (-1 = disabled, full causal)
+//
+// src layout:
+//   src[0] = q                [head_dim, n_q_tokens, n_q_heads, n_req]
+//   src[1] = k_pool           [head_dim * n_kv_heads, total_pages*page_size]
+//   src[2] = v_pool           (same shape as k_pool)
+//   src[3] = page_indices     [total_pages_in_batch] I32
+//   src[4] = page_indptr      [n_req + 1] I32
+//   src[5] = last_page_lens   [n_req] I32
+struct ggml_tensor * ggml_paged_attn_ext(
+        struct ggml_context * ctx,
+        struct ggml_tensor  * q,
+        struct ggml_tensor  * k_pool,
+        struct ggml_tensor  * v_pool,
+        struct ggml_tensor  * page_indices,
+        struct ggml_tensor  * page_indptr,
+        struct ggml_tensor  * last_page_lens,
+        int                   page_size,
+        int                   head_dim,
+        int                   n_kv_heads,
+        int                   sliding_window,
+        float                 scale,
+        float                 softcap) {
+    GGML_ASSERT(k_pool->ne[0] == (int64_t) head_dim * n_kv_heads);
+    GGML_ASSERT(v_pool->ne[0] == (int64_t) head_dim * n_kv_heads);
+    GGML_ASSERT(page_indices->type   == GGML_TYPE_I32);
+    GGML_ASSERT(page_indptr->type    == GGML_TYPE_I32);
+    GGML_ASSERT(last_page_lens->type == GGML_TYPE_I32);
+    GGML_ASSERT(page_indptr->ne[0]    == q->ne[3] + 1);
+    GGML_ASSERT(last_page_lens->ne[0] == q->ne[3]);
+
+    // Output shape mirrors flash_attn_ext: [v_head_dim, n_q_heads, n_q_tokens, n_req].
+    // Q is laid out as [head_dim, n_q_tokens, n_q_heads, n_req] so we permute
+    // ne[1] and ne[2] to put n_q_heads first.
+    int64_t ne[4] = { q->ne[0], q->ne[2], q->ne[1], q->ne[3] };
+    struct ggml_tensor * result = ggml_new_tensor(ctx, q->type, 4, ne);
+
+    int32_t params_i32[6];
+    memcpy(&params_i32[0], &scale,   sizeof(float));
+    memcpy(&params_i32[1], &softcap, sizeof(float));
+    params_i32[2] = page_size;
+    params_i32[3] = head_dim;
+    params_i32[4] = n_kv_heads;
+    params_i32[5] = sliding_window;
+    ggml_set_op_params(result, params_i32, sizeof(params_i32));
+
+    result->op     = GGML_OP_PAGED_ATTN_EXT;
+    result->src[0] = q;
+    result->src[1] = k_pool;
+    result->src[2] = v_pool;
+    result->src[3] = page_indices;
+    result->src[4] = page_indptr;
+    result->src[5] = last_page_lens;
+
+    return result;
+}
+
 // ggml_flash_attn_back
 
 struct ggml_tensor * ggml_flash_attn_back(
--- a/ggml/src/ggml-cuda/ggml-cuda.cu
+++ b/ggml/src/ggml-cuda/ggml-cuda.cu
@@ -1,6 +1,30 @@
 #include "ggml-cuda.h"
 #include "ggml-impl.h"
 #include "ggml-backend-impl.h"
+
+// pie patch: out-of-tree paged-attention hook.
+//
+// The kernel implementation lives in the consumer (Pie) and is registered
+// at startup via ggml_cuda_register_paged_attn_ext (declared at the
+// bottom of this file). Until a consumer registers, supports_op returns
+// false for GGML_OP_PAGED_ATTN_EXT and the dispatch case is unreachable
+// — callers fall back to the materialize + flash_attn_ext path.
+//
+// Keeping the kernel out-of-tree avoids dragging FlashInfer / cuTLASS
+// into ggml's build and keeps the patch surface narrow.
+typedef void (*pie_paged_attn_dispatch_fn)(cudaStream_t stream,
+                                           ggml_tensor * dst);
+typedef bool (*pie_paged_attn_supported_fn)(int device,
+                                            const ggml_tensor * dst);
+static pie_paged_attn_dispatch_fn  g_pie_paged_dispatch  = NULL;
+static pie_paged_attn_supported_fn g_pie_paged_supported = NULL;
+
+extern "C" GGML_BACKEND_API void ggml_cuda_register_paged_attn_ext(
+        pie_paged_attn_dispatch_fn  dispatch,
+        pie_paged_attn_supported_fn supported) {
+    g_pie_paged_dispatch  = dispatch;
+    g_pie_paged_supported = supported;
+}
 
 #include "ggml-cuda/common.cuh"
 #include "ggml-cuda/acc.cuh"
@@ -2918,6 +2942,13 @@
             break;
         case GGML_OP_FLASH_ATTN_EXT:
             ggml_cuda_flash_attn_ext(ctx, dst);
+            break;
+        case GGML_OP_PAGED_ATTN_EXT:
+            // Out-of-tree consumer-registered kernel. supports_op below
+            // gates this case, so reaching it without a registered fn is
+            // a logic error in the scheduler.
+            GGML_ASSERT(g_pie_paged_dispatch != NULL);
+            g_pie_paged_dispatch(ctx.stream(), dst);
             break;
         case GGML_OP_CROSS_ENTROPY_LOSS:
             ggml_cuda_cross_entropy_loss(ctx, dst);
@@ -5203,6 +5234,9 @@
 #endif // GGML_USE_MUSA
         case GGML_OP_FLASH_ATTN_EXT:
             return ggml_cuda_flash_attn_ext_supported(dev_ctx->device, op);
+        case GGML_OP_PAGED_ATTN_EXT:
+            return g_pie_paged_supported != NULL &&
+                   g_pie_paged_supported(dev_ctx->device, op);
         case GGML_OP_CROSS_ENTROPY_LOSS:
         case GGML_OP_CROSS_ENTROPY_LOSS_BACK:
         case GGML_OP_OPT_STEP_ADAMW:
--- a/ggml/src/ggml-cpu/ggml-cpu.cpp
+++ b/ggml/src/ggml-cpu/ggml-cpu.cpp
@@ -470,4 +470,6 @@
                 src1->type == GGML_TYPE_F32 && op->type == GGML_TYPE_F32;
+        case GGML_OP_PAGED_ATTN_EXT:
+            return false;
         default:
             return true;
     }
