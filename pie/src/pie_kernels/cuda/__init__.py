"""pie_kernels.cuda: CUDA/Triton kernels owned by pie.

This package holds CUDA code that is *not* part of upstream flashinfer. Upstream
flashinfer remains a direct dependency and is consumed via `pie_kernels`.
"""
