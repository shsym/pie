[package]
name = "agent-react"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
inferlet = { path = "../../sdk/rust/inferlet" }
evalexpr = "11.3"
chrono = "0.4"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
lto = true
