[package]
name = "agent-codeact"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
inferlet = { path = "../../sdk/rust/inferlet" }
boa_engine = "0.20.0"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
lto = true
