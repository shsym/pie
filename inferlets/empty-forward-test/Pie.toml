[package]
name = "empty-forward-test"
version = "0.1.0"
description = "Verifies the runtime API rejects empty-input forward passes"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

[parameters]
