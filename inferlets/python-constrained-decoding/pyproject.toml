[project]
name = "python-constrained-decoding"
version = "0.1.0"
description = "Grammar-constrained JSON generation in Python: Schema.json_schema + pydantic"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
]
