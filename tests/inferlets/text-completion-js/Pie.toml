[package]
name = "text-completion-js"
version = "0.1.0"
description = "Greedy text completion, host-driven — the JavaScript twin of text-completion"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"

[parameters]
prompt = {type = "string", optional = true, description = "Prompt to continue"}
max_tokens = {type = "int", optional = true, description = "Tokens to generate (default: 8)"}
