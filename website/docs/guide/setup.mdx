---
title: Initial setup
description: Configure Pie, download a model, and run your first prompt
sidebar_position: 2
---

# Initial setup

This page configures Pie after installation and runs a sample prompt to verify the engine boots end-to-end. Read this after [Installation](./install).

## Initialize the config

```bash
pie config init
```

`pie config init` writes a default `~/.pie/config.toml` and downloads the Python 3.14 inferlet runtime into `~/.pie/`. The TOML it writes:

```toml
[server]
host = "127.0.0.1"
port = 8080

[auth]
enabled = false

[model.default]
hf_repo = "Qwen/Qwen3-0.6B"

[model.default.driver]
type = "native"
device = ["cuda:0"]   # or ["mps"] / ["cpu"] depending on the host
```

The default config picks up the right device for your platform. See [Configuration](../reference/configuration) for the full schema.

## Download a model

The default config points at `Qwen/Qwen3-0.6B`, a 600M-parameter model that fits on most GPUs and on Apple Silicon laptops. Download it:

```bash
pie model download Qwen/Qwen3-0.6B
```

Other compatible repos:

```bash
pie model download Qwen/Qwen2.5-7B-Instruct
pie model download meta-llama/Llama-3.2-1B-Instruct
```

`pie model list` reports compatibility. Architectures the native driver supports get a `✓`. The full list of supported architectures is in [Supported models](../reference/models).

To point the config at a different model:

```bash
pie config set model.default.hf_repo Qwen/Qwen2.5-7B-Instruct
```

## Run your first prompt

```bash
pie run text-completion --prompt "The capital of France is"
```

```
╭─ Pie Run ───────────────────────────────────────╮
│ Inferlet     text-completion@0.1.0              │
│ Model        default (Qwen/Qwen3-0.6B)          │
│ Driver       native                             │
│ Device       cuda:0                             │
╰─────────────────────────────────────────────────╯

The capital of France is Paris, which is located in…
```

`pie run` boots a one-shot engine, runs the inferlet, prints its output, and exits. The first run is slower because of JIT and kernel warmup.

### What the command does

| Part | Meaning |
|------|---------|
| `pie run` | Boot a one-shot engine, run an inferlet, exit. |
| `text-completion` | The inferlet to run. Resolved to `text-completion@<latest>` via the registry. |
| `--prompt "…"` | Becomes `{"prompt": "…"}` in the inferlet's input JSON. |

Flags after the inferlet name fold into the input dict. Type inference handles ints, floats, and booleans automatically:

```bash
pie run text-completion \
  --prompt "Write a haiku about pie" \
  --max-tokens 64 \
  --temperature 0.7
```

### Pin a version

```bash
pie run text-completion@0.1.0 --prompt "hi"
```

Without `@`, Pie resolves to the latest version in the registry.

## Inspect or edit the config

```bash
pie config show              # pretty-print ~/.pie/config.toml
pie config set server.port 9090
pie config set model.default.hf_repo Qwen/Qwen2.5-3B-Instruct
```

## Diagnose with doctor

If something looks wrong, run `pie doctor`:

```bash
pie doctor
```

It reports your platform, GPU/MPS availability, key Python package versions, and whether the config and default model are present.

## Next

- [Development environment](./dev-env): install the toolchain to write your own inferlets.
- [Tutorial](./tutorial/build): build a parallel research agent end-to-end.
- [Your first inferlet](./first-inferlet): a minimal Rust inferlet with build and run.
