---
title: Examples
description: Annotated inferlet implementations
sidebar_position: 6
---

# Examples

Every inferlet in this list is a complete, runnable project under
[`inferlets/`](https://github.com/pie-project/pie/tree/main/inferlets).
Click the name to jump to the source.

Build & run any of them with `pie run`:

```bash
pie run --path target/wasm32-wasip2/release/<name>.wasm \
        --manifest inferlets/<name>/Pie.toml \
        --prompt "..."
```

Or for the published versions, just `pie run <name> --prompt "..."`.

## Chat & generation

| Inferlet | What it shows |
|---|---|
| [`helloworld`](https://github.com/pie-project/pie/tree/main/inferlets/helloworld) | Minimal `#[inferlet::main]` with typed JSON input/output. |
| [`text-completion`](https://github.com/pie-project/pie/tree/main/inferlets/text-completion) | Chat-style generation via `Context::system / user / cue` and `EventStream` with reasoning decoding. |
| [`text-completion-spec`](https://github.com/pie-project/pie/tree/main/inferlets/text-completion-spec) | Same shape, but exercises the speculative-decoding path (`Speculation::Default` → backend NGRAM). |
| [`python-example`](https://github.com/pie-project/pie/tree/main/inferlets/python-example) | Text completion in Python. |
| [`js-example`](https://github.com/pie-project/pie/tree/main/inferlets/js-example) | Text completion in JavaScript. |

## Sampler outputs (advanced)

| Inferlet | What it shows |
|---|---|
| [`sampler-suite`](https://github.com/pie-project/pie/tree/main/inferlets/sampler-suite) | End-to-end correctness check for every sampler kind (token / distribution / raw-logits / logprob / logprobs / entropy) attached to the same forward-pass slot. |
| [`raw-logits-demo`](https://github.com/pie-project/pie/tree/main/inferlets/raw-logits-demo) | Microbench for `Sampler::raw_logits()` — measures per-call overhead vs greedy. |
| [`output-validation`](https://github.com/pie-project/pie/tree/main/inferlets/output-validation) | Score candidate strings by their normalized log-probability (yes/no, A/B/C/D). |
| [`best-of-n`](https://github.com/pie-project/pie/tree/main/inferlets/best-of-n) | Best-of-N generation with diversity ranking. |
| [`watermarking`](https://github.com/pie-project/pie/tree/main/inferlets/watermarking) | Text watermarking via green/red token partitioning. |

## Structured generation

| Inferlet | What it shows |
|---|---|
| [`constrained-decoding`](https://github.com/pie-project/pie/tree/main/inferlets/constrained-decoding) | Grammar-constrained decoding with a Lark grammar. |
| [`json-schema-validation`](https://github.com/pie-project/pie/tree/main/inferlets/json-schema-validation) | JSON generation constrained against a runtime-string JSON schema. |
| [`template-generation`](https://github.com/pie-project/pie/tree/main/inferlets/template-generation) | Grammar-constrained JSON merged with a Jinja2 template. |
| [`python-constrained-decoding`](https://github.com/pie-project/pie/tree/main/inferlets/python-constrained-decoding) | Same idea in Python. |
| [`js-constrained-decoding`](https://github.com/pie-project/pie/tree/main/inferlets/js-constrained-decoding) | Same idea in JavaScript. |

## KV cache & attention

| Inferlet | What it shows |
|---|---|
| [`parallel-generation`](https://github.com/pie-project/pie/tree/main/inferlets/parallel-generation) | Forked contexts that share committed pages and decode in parallel. |
| [`prefix-tree`](https://github.com/pie-project/pie/tree/main/inferlets/prefix-tree) | Prefix-tree caching with concurrent generation from one shared context. |
| [`windowed-attention`](https://github.com/pie-project/pie/tree/main/inferlets/windowed-attention) | Sliding window: bounded-memory generation by masking + releasing pages. |
| [`attention-sink`](https://github.com/pie-project/pie/tree/main/inferlets/attention-sink) | Sink + sliding window (StreamingLLM). |
| [`page-trim-bench`](https://github.com/pie-project/pie/tree/main/inferlets/page-trim-bench) | Benchmarks the runtime's [page-trim](./writing-inferlets/page-trim) optimization on a sink+window mask. |

## Speculative decoding

| Inferlet | What it shows |
|---|---|
| [`jacobi-decoding`](https://github.com/pie-project/pie/tree/main/inferlets/jacobi-decoding) | Parallel Jacobi decoding (custom drafter). |
| [`cacheback-decoding`](https://github.com/pie-project/pie/tree/main/inferlets/cacheback-decoding) | Cache-based drafter via n-gram matching. |

## Reasoning patterns

| Inferlet | What it shows |
|---|---|
| [`tree-of-thought`](https://github.com/pie-project/pie/tree/main/inferlets/tree-of-thought) | Multi-branch reasoning (Propose, Execute, Reflect). |
| [`graph-of-thought`](https://github.com/pie-project/pie/tree/main/inferlets/graph-of-thought) | Hierarchical aggregation of multiple proposals. |
| [`skeleton-of-thought`](https://github.com/pie-project/pie/tree/main/inferlets/skeleton-of-thought) | Parallel elaboration of key points. |
| [`recursion-of-thought`](https://github.com/pie-project/pie/tree/main/inferlets/recursion-of-thought) | Divide-and-conquer problem solving. |
| [`knowledge-graph`](https://github.com/pie-project/pie/tree/main/inferlets/knowledge-graph) | Knowledge-graph extraction and traversal with `petgraph`. |

## Agents

| Inferlet | What it shows |
|---|---|
| [`agent-react`](https://github.com/pie-project/pie/tree/main/inferlets/agent-react) | ReAct (Reasoning + Acting) loop with tool execution. |
| [`agent-codeact`](https://github.com/pie-project/pie/tree/main/inferlets/agent-codeact) | CodeACT — JavaScript code execution as the action space. |
| [`agent-swarm`](https://github.com/pie-project/pie/tree/main/inferlets/agent-swarm) | Collaborative agent swarm coordinating via `messaging::broadcast/subscribe`. |

## System integration

| Inferlet | What it shows |
|---|---|
| [`http-server`](https://github.com/pie-project/pie/tree/main/inferlets/http-server) | `wasi:http/incoming-handler` server with SSE streaming. |
| [`image-fetch`](https://github.com/pie-project/pie/tree/main/inferlets/image-fetch) | `wstd::http::Client` to fetch and decode images. |
| [`openresponses`](https://github.com/pie-project/pie/tree/main/inferlets/openresponses) | OpenResponses-compliant HTTP API. |

## Concurrency

| Inferlet | What it shows |
|---|---|
| [`py-concurrency-test`](https://github.com/pie-project/pie/tree/main/inferlets/py-concurrency-test) | Concurrency test for the Python SDK. |
| [`js-concurrency-test`](https://github.com/pie-project/pie/tree/main/inferlets/js-concurrency-test) | Concurrency test for the JS SDK. |

## See also

- [Writing inferlets](./writing-inferlets) — the guides these examples illustrate.
- [Agentic patterns](./agentic-patterns) — the architectural patterns, abstracted from any one example.
