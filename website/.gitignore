# Dependencies
/node_modules

# Production
/build

# Re-include the docs/build/ subtree (the parent repo's .gitignore has a
# generic `build/` rule that would otherwise hide it).
!docs/build/
!docs/build/**

# Generated files
.docusaurus
.cache-loader
# Copied from scripts/install.* by the prestart/prebuild npm scripts.
static/install.sh
static/install.ps1
static/install.cmd

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
node_modules
