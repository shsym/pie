#pragma once
// Stub csrc/src/model/loaded_model.hpp: llama_like.cpp names LoadedModel only
// in qwen3.hpp declarations, so the real HfConfig plus a forward declaration
// is the whole surface.
#include "model/config.hpp"
namespace pie_cuda_driver { class LoadedModel; }
