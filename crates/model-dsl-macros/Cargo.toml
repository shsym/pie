[package]
name = "model-dsl-macros"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/pie-project/pie"
description = "The Facts derive: one Predicate constructor and one word bit per declared fact."

[lib]
proc-macro = true

[dependencies]
syn = { version = "3.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
